# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1CronJobSpec
#'
#' CronJobSpec describes how the job execution will look like and when it will actually run.
#' @name V1CronJobSpec
#' @noMd
#' @export
V1CronJobSpec <- R6::R6Class(
  "V1CronJobSpec",
  public = list(
    `concurrency_policy` = NULL,
    `failed_jobs_history_limit` = NULL,
    `job_template` = NULL,
    `schedule` = NULL,
    `starting_deadline_seconds` = NULL,
    `successful_jobs_history_limit` = NULL,
    `suspend` = NULL,
    `time_zone` = NULL
    ,
    #' @description Construct.
    initialize = function(`concurrency_policy` = NULL, `failed_jobs_history_limit` = NULL, `job_template` = NULL, `schedule` = NULL, `starting_deadline_seconds` = NULL, `successful_jobs_history_limit` = NULL, `suspend` = NULL, `time_zone` = NULL) {
      self$`concurrency_policy` <- `concurrency_policy`
      self$`failed_jobs_history_limit` <- `failed_jobs_history_limit`
      self$`job_template` <- `job_template`
      self$`schedule` <- `schedule`
      self$`starting_deadline_seconds` <- `starting_deadline_seconds`
      self$`successful_jobs_history_limit` <- `successful_jobs_history_limit`
      self$`suspend` <- `suspend`
      self$`time_zone` <- `time_zone`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["concurrencyPolicy"]] <- self$`concurrency_policy`
      out[["failedJobsHistoryLimit"]] <- self$`failed_jobs_history_limit`
      out[["jobTemplate"]] <- if (!is.null(self$`job_template`)) self$`job_template`$to_list() else NULL
      out[["schedule"]] <- self$`schedule`
      out[["startingDeadlineSeconds"]] <- self$`starting_deadline_seconds`
      out[["successfulJobsHistoryLimit"]] <- self$`successful_jobs_history_limit`
      out[["suspend"]] <- self$`suspend`
      out[["timeZone"]] <- self$`time_zone`
      drop_nulls_deep(out)
    }
  )
)

V1CronJobSpec$from_list <- function(x) {
  V1CronJobSpec$new(
    `concurrency_policy` = x[["concurrencyPolicy"]],
    `failed_jobs_history_limit` = x[["failedJobsHistoryLimit"]],
    `job_template` = if (is.null(x[["jobTemplate"]])) NULL else V1JobTemplateSpec$from_list(x[["jobTemplate"]]),
    `schedule` = x[["schedule"]],
    `starting_deadline_seconds` = x[["startingDeadlineSeconds"]],
    `successful_jobs_history_limit` = x[["successfulJobsHistoryLimit"]],
    `suspend` = x[["suspend"]],
    `time_zone` = x[["timeZone"]]
  )
}

V1CronJobSpec$required_fields <- c("schedule", "jobTemplate")

