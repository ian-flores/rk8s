# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1QuobyteVolumeSource
#'
#' Represents a Quobyte mount that lasts the lifetime of a pod. Quobyte volumes do not support ownership management or SELinux relabeling.
#' @name V1QuobyteVolumeSource
#' @noMd
#' @export
V1QuobyteVolumeSource <- R6::R6Class(
  "V1QuobyteVolumeSource",
  public = list(
    `group` = NULL,
    `read_only` = NULL,
    `registry` = NULL,
    `tenant` = NULL,
    `user` = NULL,
    `volume` = NULL
    ,
    #' @description Construct.
    initialize = function(`group` = NULL, `read_only` = NULL, `registry` = NULL, `tenant` = NULL, `user` = NULL, `volume` = NULL) {
      self$`group` <- `group`
      self$`read_only` <- `read_only`
      self$`registry` <- `registry`
      self$`tenant` <- `tenant`
      self$`user` <- `user`
      self$`volume` <- `volume`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["group"]] <- self$`group`
      out[["readOnly"]] <- self$`read_only`
      out[["registry"]] <- self$`registry`
      out[["tenant"]] <- self$`tenant`
      out[["user"]] <- self$`user`
      out[["volume"]] <- self$`volume`
      drop_nulls_deep(out)
    }
  )
)

V1QuobyteVolumeSource$from_list <- function(x) {
  V1QuobyteVolumeSource$new(
    `group` = x[["group"]],
    `read_only` = x[["readOnly"]],
    `registry` = x[["registry"]],
    `tenant` = x[["tenant"]],
    `user` = x[["user"]],
    `volume` = x[["volume"]]
  )
}

V1QuobyteVolumeSource$required_fields <- c("registry", "volume")

