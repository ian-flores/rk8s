# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1alpha3ResourceClaimStatus
#'
#' ResourceClaimStatus tracks whether the resource has been allocated and what the result of that was.
#' @name V1alpha3ResourceClaimStatus
#' @noMd
#' @export
V1alpha3ResourceClaimStatus <- R6::R6Class(
  "V1alpha3ResourceClaimStatus",
  public = list(
    `allocation` = NULL,
    `deallocation_requested` = NULL,
    `reserved_for` = NULL
    ,
    #' @description Construct.
    initialize = function(`allocation` = NULL, `deallocation_requested` = NULL, `reserved_for` = NULL) {
      self$`allocation` <- `allocation`
      self$`deallocation_requested` <- `deallocation_requested`
      self$`reserved_for` <- `reserved_for`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["allocation"]] <- if (!is.null(self$`allocation`)) self$`allocation`$to_list() else NULL
      out[["deallocationRequested"]] <- self$`deallocation_requested`
      out[["reservedFor"]] <- if (!is.null(self$`reserved_for`)) lapply(self$`reserved_for`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1alpha3ResourceClaimStatus$from_list <- function(x) {
  V1alpha3ResourceClaimStatus$new(
    `allocation` = if (is.null(x[["allocation"]])) NULL else V1alpha3AllocationResult$from_list(x[["allocation"]]),
    `deallocation_requested` = x[["deallocationRequested"]],
    `reserved_for` = if (is.null(x[["reservedFor"]])) NULL else lapply(x[["reservedFor"]], V1alpha3ResourceClaimConsumerReference$from_list)
  )
}

