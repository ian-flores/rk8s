# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1SecretVolumeSource
#'
#' Adapts a Secret into a volume.
#' 
#' The contents of the target Secret's Data field will be presented in a volume as files using the keys in the Data field as the file names. Secret volumes support ownership management and SELinux relabeling.
#' @name V1SecretVolumeSource
#' @noMd
#' @export
V1SecretVolumeSource <- R6::R6Class(
  "V1SecretVolumeSource",
  public = list(
    `default_mode` = NULL,
    `items` = NULL,
    `optional` = NULL,
    `secret_name` = NULL
    ,
    #' @description Construct.
    initialize = function(`default_mode` = NULL, `items` = NULL, `optional` = NULL, `secret_name` = NULL) {
      self$`default_mode` <- `default_mode`
      self$`items` <- `items`
      self$`optional` <- `optional`
      self$`secret_name` <- `secret_name`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["defaultMode"]] <- self$`default_mode`
      out[["items"]] <- if (!is.null(self$`items`)) lapply(self$`items`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["optional"]] <- self$`optional`
      out[["secretName"]] <- self$`secret_name`
      drop_nulls_deep(out)
    }
  )
)

V1SecretVolumeSource$from_list <- function(x) {
  V1SecretVolumeSource$new(
    `default_mode` = x[["defaultMode"]],
    `items` = if (is.null(x[["items"]])) NULL else lapply(x[["items"]], V1KeyToPath$from_list),
    `optional` = x[["optional"]],
    `secret_name` = x[["secretName"]]
  )
}

