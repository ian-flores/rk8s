# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1ServiceAccount
#'
#' ServiceAccount binds together: * a name, understood by users, and perhaps by peripheral systems, for an identity * a principal that can be authenticated and authorized * a set of secrets
#' @name V1ServiceAccount
#' @noMd
#' @export
V1ServiceAccount <- R6::R6Class(
  "V1ServiceAccount",
  public = list(
    `api_version` = NULL,
    `automount_service_account_token` = NULL,
    `image_pull_secrets` = NULL,
    `kind` = NULL,
    `metadata` = NULL,
    `secrets` = NULL
    ,
    #' @description Construct.
    initialize = function(`api_version` = NULL, `automount_service_account_token` = NULL, `image_pull_secrets` = NULL, `kind` = NULL, `metadata` = NULL, `secrets` = NULL) {
      self$`api_version` <- `api_version`
      self$`automount_service_account_token` <- `automount_service_account_token`
      self$`image_pull_secrets` <- `image_pull_secrets`
      self$`kind` <- `kind`
      self$`metadata` <- `metadata`
      self$`secrets` <- `secrets`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["apiVersion"]] <- self$`api_version`
      out[["automountServiceAccountToken"]] <- self$`automount_service_account_token`
      out[["imagePullSecrets"]] <- if (!is.null(self$`image_pull_secrets`)) lapply(self$`image_pull_secrets`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["kind"]] <- self$`kind`
      out[["metadata"]] <- if (!is.null(self$`metadata`)) self$`metadata`$to_list() else NULL
      out[["secrets"]] <- if (!is.null(self$`secrets`)) lapply(self$`secrets`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1ServiceAccount$from_list <- function(x) {
  V1ServiceAccount$new(
    `api_version` = x[["apiVersion"]],
    `automount_service_account_token` = x[["automountServiceAccountToken"]],
    `image_pull_secrets` = if (is.null(x[["imagePullSecrets"]])) NULL else lapply(x[["imagePullSecrets"]], V1LocalObjectReference$from_list),
    `kind` = x[["kind"]],
    `metadata` = if (is.null(x[["metadata"]])) NULL else V1ObjectMeta$from_list(x[["metadata"]]),
    `secrets` = if (is.null(x[["secrets"]])) NULL else lapply(x[["secrets"]], V1ObjectReference$from_list)
  )
}

