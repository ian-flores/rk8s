# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1ResourceRequirements
#'
#' ResourceRequirements describes the compute resource requirements.
#' @name V1ResourceRequirements
#' @noMd
#' @export
V1ResourceRequirements <- R6::R6Class(
  "V1ResourceRequirements",
  public = list(
    `claims` = NULL,
    `limits` = NULL,
    `requests` = NULL
    ,
    #' @description Construct.
    initialize = function(`claims` = NULL, `limits` = NULL, `requests` = NULL) {
      self$`claims` <- `claims`
      self$`limits` <- `limits`
      self$`requests` <- `requests`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["claims"]] <- if (!is.null(self$`claims`)) lapply(self$`claims`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["limits"]] <- if (!is.null(self$`limits`)) lapply(self$`limits`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["requests"]] <- if (!is.null(self$`requests`)) lapply(self$`requests`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1ResourceRequirements$from_list <- function(x) {
  V1ResourceRequirements$new(
    `claims` = if (is.null(x[["claims"]])) NULL else lapply(x[["claims"]], V1ResourceClaim$from_list),
    `limits` = if (is.null(x[["limits"]])) NULL else lapply(x[["limits"]], Quantity$from_list),
    `requests` = if (is.null(x[["requests"]])) NULL else lapply(x[["requests"]], Quantity$from_list)
  )
}

