# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1PersistentVolumeClaimVolumeSource
#'
#' PersistentVolumeClaimVolumeSource references the user's PVC in the same namespace. This volume finds the bound PV and mounts that volume for the pod. A PersistentVolumeClaimVolumeSource is, essentially, a wrapper around another type of volume that is owned by someone else (the system).
#' @name V1PersistentVolumeClaimVolumeSource
#' @noMd
#' @export
V1PersistentVolumeClaimVolumeSource <- R6::R6Class(
  "V1PersistentVolumeClaimVolumeSource",
  public = list(
    `claim_name` = NULL,
    `read_only` = NULL
    ,
    #' @description Construct.
    initialize = function(`claim_name` = NULL, `read_only` = NULL) {
      self$`claim_name` <- `claim_name`
      self$`read_only` <- `read_only`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["claimName"]] <- self$`claim_name`
      out[["readOnly"]] <- self$`read_only`
      drop_nulls_deep(out)
    }
  )
)

V1PersistentVolumeClaimVolumeSource$from_list <- function(x) {
  V1PersistentVolumeClaimVolumeSource$new(
    `claim_name` = x[["claimName"]],
    `read_only` = x[["readOnly"]]
  )
}

V1PersistentVolumeClaimVolumeSource$required_fields <- c("claimName")

