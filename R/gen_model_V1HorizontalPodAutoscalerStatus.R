# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1HorizontalPodAutoscalerStatus
#'
#' current status of a horizontal pod autoscaler
#' @name V1HorizontalPodAutoscalerStatus
#' @noMd
#' @export
V1HorizontalPodAutoscalerStatus <- R6::R6Class(
  "V1HorizontalPodAutoscalerStatus",
  public = list(
    `current_cpu_utilization_percentage` = NULL,
    `current_replicas` = NULL,
    `desired_replicas` = NULL,
    `last_scale_time` = NULL,
    `observed_generation` = NULL
    ,
    #' @description Construct.
    initialize = function(`current_cpu_utilization_percentage` = NULL, `current_replicas` = NULL, `desired_replicas` = NULL, `last_scale_time` = NULL, `observed_generation` = NULL) {
      self$`current_cpu_utilization_percentage` <- `current_cpu_utilization_percentage`
      self$`current_replicas` <- `current_replicas`
      self$`desired_replicas` <- `desired_replicas`
      self$`last_scale_time` <- `last_scale_time`
      self$`observed_generation` <- `observed_generation`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["currentCPUUtilizationPercentage"]] <- self$`current_cpu_utilization_percentage`
      out[["currentReplicas"]] <- self$`current_replicas`
      out[["desiredReplicas"]] <- self$`desired_replicas`
      out[["lastScaleTime"]] <- if (!is.null(self$`last_scale_time`)) self$`last_scale_time`$to_list() else NULL
      out[["observedGeneration"]] <- self$`observed_generation`
      drop_nulls_deep(out)
    }
  )
)

V1HorizontalPodAutoscalerStatus$from_list <- function(x) {
  V1HorizontalPodAutoscalerStatus$new(
    `current_cpu_utilization_percentage` = x[["currentCPUUtilizationPercentage"]],
    `current_replicas` = x[["currentReplicas"]],
    `desired_replicas` = x[["desiredReplicas"]],
    `last_scale_time` = if (is.null(x[["lastScaleTime"]])) NULL else V1Time$from_list(x[["lastScaleTime"]]),
    `observed_generation` = x[["observedGeneration"]]
  )
}

V1HorizontalPodAutoscalerStatus$required_fields <- c("currentReplicas", "desiredReplicas")

