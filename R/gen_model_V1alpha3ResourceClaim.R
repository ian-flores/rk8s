# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1alpha3ResourceClaim
#'
#' ResourceClaim describes a request for access to resources in the cluster, for use by workloads. For example, if a workload needs an accelerator device with specific properties, this is how that request is expressed. The status stanza tracks whether this claim has been satisfied and what specific resources have been allocated.
#' 
#' This is an alpha type and requires enabling the DynamicResourceAllocation feature gate.
#' @name V1alpha3ResourceClaim
#' @noMd
#' @export
V1alpha3ResourceClaim <- R6::R6Class(
  "V1alpha3ResourceClaim",
  public = list(
    `api_version` = NULL,
    `kind` = NULL,
    `metadata` = NULL,
    `spec` = NULL,
    `status` = NULL
    ,
    #' @description Construct.
    initialize = function(`api_version` = NULL, `kind` = NULL, `metadata` = NULL, `spec` = NULL, `status` = NULL) {
      self$`api_version` <- `api_version`
      self$`kind` <- `kind`
      self$`metadata` <- `metadata`
      self$`spec` <- `spec`
      self$`status` <- `status`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["apiVersion"]] <- self$`api_version`
      out[["kind"]] <- self$`kind`
      out[["metadata"]] <- if (!is.null(self$`metadata`)) self$`metadata`$to_list() else NULL
      out[["spec"]] <- if (!is.null(self$`spec`)) self$`spec`$to_list() else NULL
      out[["status"]] <- if (!is.null(self$`status`)) self$`status`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V1alpha3ResourceClaim$from_list <- function(x) {
  V1alpha3ResourceClaim$new(
    `api_version` = x[["apiVersion"]],
    `kind` = x[["kind"]],
    `metadata` = if (is.null(x[["metadata"]])) NULL else V1ObjectMeta$from_list(x[["metadata"]]),
    `spec` = if (is.null(x[["spec"]])) NULL else V1alpha3ResourceClaimSpec$from_list(x[["spec"]]),
    `status` = if (is.null(x[["status"]])) NULL else V1alpha3ResourceClaimStatus$from_list(x[["status"]])
  )
}

V1alpha3ResourceClaim$required_fields <- c("spec")

