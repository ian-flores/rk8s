# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1ResourceQuotaSpec
#'
#' ResourceQuotaSpec defines the desired hard limits to enforce for Quota.
#' @name V1ResourceQuotaSpec
#' @noMd
#' @export
V1ResourceQuotaSpec <- R6::R6Class(
  "V1ResourceQuotaSpec",
  public = list(
    `hard` = NULL,
    `scope_selector` = NULL,
    `scopes` = NULL
    ,
    #' @description Construct.
    initialize = function(`hard` = NULL, `scope_selector` = NULL, `scopes` = NULL) {
      self$`hard` <- `hard`
      self$`scope_selector` <- `scope_selector`
      self$`scopes` <- `scopes`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["hard"]] <- if (!is.null(self$`hard`)) lapply(self$`hard`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["scopeSelector"]] <- if (!is.null(self$`scope_selector`)) self$`scope_selector`$to_list() else NULL
      out[["scopes"]] <- self$`scopes`
      drop_nulls_deep(out)
    }
  )
)

V1ResourceQuotaSpec$from_list <- function(x) {
  V1ResourceQuotaSpec$new(
    `hard` = if (is.null(x[["hard"]])) NULL else lapply(x[["hard"]], Quantity$from_list),
    `scope_selector` = if (is.null(x[["scopeSelector"]])) NULL else V1ScopeSelector$from_list(x[["scopeSelector"]]),
    `scopes` = x[["scopes"]]
  )
}

