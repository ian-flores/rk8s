# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1DaemonSetStatus
#'
#' DaemonSetStatus represents the current status of a daemon set.
#' @name V1DaemonSetStatus
#' @noMd
#' @export
V1DaemonSetStatus <- R6::R6Class(
  "V1DaemonSetStatus",
  public = list(
    `collision_count` = NULL,
    `conditions` = NULL,
    `current_number_scheduled` = NULL,
    `desired_number_scheduled` = NULL,
    `number_available` = NULL,
    `number_misscheduled` = NULL,
    `number_ready` = NULL,
    `number_unavailable` = NULL,
    `observed_generation` = NULL,
    `updated_number_scheduled` = NULL
    ,
    #' @description Construct.
    initialize = function(`collision_count` = NULL, `conditions` = NULL, `current_number_scheduled` = NULL, `desired_number_scheduled` = NULL, `number_available` = NULL, `number_misscheduled` = NULL, `number_ready` = NULL, `number_unavailable` = NULL, `observed_generation` = NULL, `updated_number_scheduled` = NULL) {
      self$`collision_count` <- `collision_count`
      self$`conditions` <- `conditions`
      self$`current_number_scheduled` <- `current_number_scheduled`
      self$`desired_number_scheduled` <- `desired_number_scheduled`
      self$`number_available` <- `number_available`
      self$`number_misscheduled` <- `number_misscheduled`
      self$`number_ready` <- `number_ready`
      self$`number_unavailable` <- `number_unavailable`
      self$`observed_generation` <- `observed_generation`
      self$`updated_number_scheduled` <- `updated_number_scheduled`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["collisionCount"]] <- self$`collision_count`
      out[["conditions"]] <- if (!is.null(self$`conditions`)) lapply(self$`conditions`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["currentNumberScheduled"]] <- self$`current_number_scheduled`
      out[["desiredNumberScheduled"]] <- self$`desired_number_scheduled`
      out[["numberAvailable"]] <- self$`number_available`
      out[["numberMisscheduled"]] <- self$`number_misscheduled`
      out[["numberReady"]] <- self$`number_ready`
      out[["numberUnavailable"]] <- self$`number_unavailable`
      out[["observedGeneration"]] <- self$`observed_generation`
      out[["updatedNumberScheduled"]] <- self$`updated_number_scheduled`
      drop_nulls_deep(out)
    }
  )
)

V1DaemonSetStatus$from_list <- function(x) {
  V1DaemonSetStatus$new(
    `collision_count` = x[["collisionCount"]],
    `conditions` = if (is.null(x[["conditions"]])) NULL else lapply(x[["conditions"]], V1DaemonSetCondition$from_list),
    `current_number_scheduled` = x[["currentNumberScheduled"]],
    `desired_number_scheduled` = x[["desiredNumberScheduled"]],
    `number_available` = x[["numberAvailable"]],
    `number_misscheduled` = x[["numberMisscheduled"]],
    `number_ready` = x[["numberReady"]],
    `number_unavailable` = x[["numberUnavailable"]],
    `observed_generation` = x[["observedGeneration"]],
    `updated_number_scheduled` = x[["updatedNumberScheduled"]]
  )
}

V1DaemonSetStatus$required_fields <- c("currentNumberScheduled", "numberMisscheduled", "desiredNumberScheduled", "numberReady")

