# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1HTTPIngressRuleValue
#'
#' HTTPIngressRuleValue is a list of http selectors pointing to backends. In the example: http://<host>/<path>?<searchpart> -> backend where where parts of the url correspond to RFC 3986, this resource will be used to match against everything after the last '/' and before the first '?' or '#'.
#' @name V1HTTPIngressRuleValue
#' @noMd
#' @export
V1HTTPIngressRuleValue <- R6::R6Class(
  "V1HTTPIngressRuleValue",
  public = list(
    `paths` = NULL
    ,
    #' @description Construct.
    initialize = function(`paths` = NULL) {
      self$`paths` <- `paths`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["paths"]] <- if (!is.null(self$`paths`)) lapply(self$`paths`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1HTTPIngressRuleValue$from_list <- function(x) {
  V1HTTPIngressRuleValue$new(
    `paths` = if (is.null(x[["paths"]])) NULL else lapply(x[["paths"]], V1HTTPIngressPath$from_list)
  )
}

V1HTTPIngressRuleValue$required_fields <- c("paths")

