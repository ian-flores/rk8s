# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1alpha1LeaseCandidateSpec
#'
#' LeaseCandidateSpec is a specification of a Lease.
#' @name V1alpha1LeaseCandidateSpec
#' @noMd
#' @export
V1alpha1LeaseCandidateSpec <- R6::R6Class(
  "V1alpha1LeaseCandidateSpec",
  public = list(
    `binary_version` = NULL,
    `emulation_version` = NULL,
    `lease_name` = NULL,
    `ping_time` = NULL,
    `preferred_strategies` = NULL,
    `renew_time` = NULL
    ,
    #' @description Construct.
    initialize = function(`binary_version` = NULL, `emulation_version` = NULL, `lease_name` = NULL, `ping_time` = NULL, `preferred_strategies` = NULL, `renew_time` = NULL) {
      self$`binary_version` <- `binary_version`
      self$`emulation_version` <- `emulation_version`
      self$`lease_name` <- `lease_name`
      self$`ping_time` <- `ping_time`
      self$`preferred_strategies` <- `preferred_strategies`
      self$`renew_time` <- `renew_time`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["binaryVersion"]] <- self$`binary_version`
      out[["emulationVersion"]] <- self$`emulation_version`
      out[["leaseName"]] <- self$`lease_name`
      out[["pingTime"]] <- if (!is.null(self$`ping_time`)) self$`ping_time`$to_list() else NULL
      out[["preferredStrategies"]] <- self$`preferred_strategies`
      out[["renewTime"]] <- if (!is.null(self$`renew_time`)) self$`renew_time`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V1alpha1LeaseCandidateSpec$from_list <- function(x) {
  V1alpha1LeaseCandidateSpec$new(
    `binary_version` = x[["binaryVersion"]],
    `emulation_version` = x[["emulationVersion"]],
    `lease_name` = x[["leaseName"]],
    `ping_time` = if (is.null(x[["pingTime"]])) NULL else V1MicroTime$from_list(x[["pingTime"]]),
    `preferred_strategies` = x[["preferredStrategies"]],
    `renew_time` = if (is.null(x[["renewTime"]])) NULL else V1MicroTime$from_list(x[["renewTime"]])
  )
}

V1alpha1LeaseCandidateSpec$required_fields <- c("leaseName", "preferredStrategies")

