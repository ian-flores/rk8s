# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1TopologySelectorTerm
#'
#' A topology selector term represents the result of label queries. A null or empty topology selector term matches no objects. The requirements of them are ANDed. It provides a subset of functionality as NodeSelectorTerm. This is an alpha feature and may change in the future.
#' @name V1TopologySelectorTerm
#' @noMd
#' @export
V1TopologySelectorTerm <- R6::R6Class(
  "V1TopologySelectorTerm",
  public = list(
    `match_label_expressions` = NULL
    ,
    #' @description Construct.
    initialize = function(`match_label_expressions` = NULL) {
      self$`match_label_expressions` <- `match_label_expressions`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["matchLabelExpressions"]] <- if (!is.null(self$`match_label_expressions`)) lapply(self$`match_label_expressions`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1TopologySelectorTerm$from_list <- function(x) {
  V1TopologySelectorTerm$new(
    `match_label_expressions` = if (is.null(x[["matchLabelExpressions"]])) NULL else lapply(x[["matchLabelExpressions"]], V1TopologySelectorLabelRequirement$from_list)
  )
}

