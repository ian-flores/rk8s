# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1alpha1StorageVersionStatus
#'
#' API server instances report the versions they can decode and the version they encode objects to when persisting objects in the backend.
#' @name V1alpha1StorageVersionStatus
#' @noMd
#' @export
V1alpha1StorageVersionStatus <- R6::R6Class(
  "V1alpha1StorageVersionStatus",
  public = list(
    `common_encoding_version` = NULL,
    `conditions` = NULL,
    `storage_versions` = NULL
    ,
    #' @description Construct.
    initialize = function(`common_encoding_version` = NULL, `conditions` = NULL, `storage_versions` = NULL) {
      self$`common_encoding_version` <- `common_encoding_version`
      self$`conditions` <- `conditions`
      self$`storage_versions` <- `storage_versions`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["commonEncodingVersion"]] <- self$`common_encoding_version`
      out[["conditions"]] <- if (!is.null(self$`conditions`)) lapply(self$`conditions`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["storageVersions"]] <- if (!is.null(self$`storage_versions`)) lapply(self$`storage_versions`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1alpha1StorageVersionStatus$from_list <- function(x) {
  V1alpha1StorageVersionStatus$new(
    `common_encoding_version` = x[["commonEncodingVersion"]],
    `conditions` = if (is.null(x[["conditions"]])) NULL else lapply(x[["conditions"]], V1alpha1StorageVersionCondition$from_list),
    `storage_versions` = if (is.null(x[["storageVersions"]])) NULL else lapply(x[["storageVersions"]], V1alpha1ServerStorageVersion$from_list)
  )
}

