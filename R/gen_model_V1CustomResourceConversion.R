# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1CustomResourceConversion
#'
#' CustomResourceConversion describes how to convert different versions of a CR.
#' @name V1CustomResourceConversion
#' @noMd
#' @export
V1CustomResourceConversion <- R6::R6Class(
  "V1CustomResourceConversion",
  public = list(
    `strategy` = NULL,
    `webhook` = NULL
    ,
    #' @description Construct.
    initialize = function(`strategy` = NULL, `webhook` = NULL) {
      self$`strategy` <- `strategy`
      self$`webhook` <- `webhook`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["strategy"]] <- self$`strategy`
      out[["webhook"]] <- if (!is.null(self$`webhook`)) self$`webhook`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V1CustomResourceConversion$from_list <- function(x) {
  V1CustomResourceConversion$new(
    `strategy` = x[["strategy"]],
    `webhook` = if (is.null(x[["webhook"]])) NULL else V1WebhookConversion$from_list(x[["webhook"]])
  )
}

V1CustomResourceConversion$required_fields <- c("strategy")

