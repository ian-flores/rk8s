# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1CronJobStatus
#'
#' CronJobStatus represents the current state of a cron job.
#' @name V1CronJobStatus
#' @noMd
#' @export
V1CronJobStatus <- R6::R6Class(
  "V1CronJobStatus",
  public = list(
    `active` = NULL,
    `last_schedule_time` = NULL,
    `last_successful_time` = NULL
    ,
    #' @description Construct.
    initialize = function(`active` = NULL, `last_schedule_time` = NULL, `last_successful_time` = NULL) {
      self$`active` <- `active`
      self$`last_schedule_time` <- `last_schedule_time`
      self$`last_successful_time` <- `last_successful_time`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["active"]] <- if (!is.null(self$`active`)) lapply(self$`active`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["lastScheduleTime"]] <- if (!is.null(self$`last_schedule_time`)) self$`last_schedule_time`$to_list() else NULL
      out[["lastSuccessfulTime"]] <- if (!is.null(self$`last_successful_time`)) self$`last_successful_time`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V1CronJobStatus$from_list <- function(x) {
  V1CronJobStatus$new(
    `active` = if (is.null(x[["active"]])) NULL else lapply(x[["active"]], V1ObjectReference$from_list),
    `last_schedule_time` = if (is.null(x[["lastScheduleTime"]])) NULL else V1Time$from_list(x[["lastScheduleTime"]]),
    `last_successful_time` = if (is.null(x[["lastSuccessfulTime"]])) NULL else V1Time$from_list(x[["lastSuccessfulTime"]])
  )
}

