# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1JobSpec
#'
#' JobSpec describes how the job execution will look like.
#' @name V1JobSpec
#' @noMd
#' @export
V1JobSpec <- R6::R6Class(
  "V1JobSpec",
  public = list(
    `active_deadline_seconds` = NULL,
    `backoff_limit` = NULL,
    `backoff_limit_per_index` = NULL,
    `completion_mode` = NULL,
    `completions` = NULL,
    `managed_by` = NULL,
    `manual_selector` = NULL,
    `max_failed_indexes` = NULL,
    `parallelism` = NULL,
    `pod_failure_policy` = NULL,
    `pod_replacement_policy` = NULL,
    `selector` = NULL,
    `success_policy` = NULL,
    `suspend` = NULL,
    `template` = NULL,
    `ttl_seconds_after_finished` = NULL
    ,
    #' @description Construct.
    initialize = function(`active_deadline_seconds` = NULL, `backoff_limit` = NULL, `backoff_limit_per_index` = NULL, `completion_mode` = NULL, `completions` = NULL, `managed_by` = NULL, `manual_selector` = NULL, `max_failed_indexes` = NULL, `parallelism` = NULL, `pod_failure_policy` = NULL, `pod_replacement_policy` = NULL, `selector` = NULL, `success_policy` = NULL, `suspend` = NULL, `template` = NULL, `ttl_seconds_after_finished` = NULL) {
      self$`active_deadline_seconds` <- `active_deadline_seconds`
      self$`backoff_limit` <- `backoff_limit`
      self$`backoff_limit_per_index` <- `backoff_limit_per_index`
      self$`completion_mode` <- `completion_mode`
      self$`completions` <- `completions`
      self$`managed_by` <- `managed_by`
      self$`manual_selector` <- `manual_selector`
      self$`max_failed_indexes` <- `max_failed_indexes`
      self$`parallelism` <- `parallelism`
      self$`pod_failure_policy` <- `pod_failure_policy`
      self$`pod_replacement_policy` <- `pod_replacement_policy`
      self$`selector` <- `selector`
      self$`success_policy` <- `success_policy`
      self$`suspend` <- `suspend`
      self$`template` <- `template`
      self$`ttl_seconds_after_finished` <- `ttl_seconds_after_finished`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["activeDeadlineSeconds"]] <- self$`active_deadline_seconds`
      out[["backoffLimit"]] <- self$`backoff_limit`
      out[["backoffLimitPerIndex"]] <- self$`backoff_limit_per_index`
      out[["completionMode"]] <- self$`completion_mode`
      out[["completions"]] <- self$`completions`
      out[["managedBy"]] <- self$`managed_by`
      out[["manualSelector"]] <- self$`manual_selector`
      out[["maxFailedIndexes"]] <- self$`max_failed_indexes`
      out[["parallelism"]] <- self$`parallelism`
      out[["podFailurePolicy"]] <- if (!is.null(self$`pod_failure_policy`)) self$`pod_failure_policy`$to_list() else NULL
      out[["podReplacementPolicy"]] <- self$`pod_replacement_policy`
      out[["selector"]] <- if (!is.null(self$`selector`)) self$`selector`$to_list() else NULL
      out[["successPolicy"]] <- if (!is.null(self$`success_policy`)) self$`success_policy`$to_list() else NULL
      out[["suspend"]] <- self$`suspend`
      out[["template"]] <- if (!is.null(self$`template`)) self$`template`$to_list() else NULL
      out[["ttlSecondsAfterFinished"]] <- self$`ttl_seconds_after_finished`
      drop_nulls_deep(out)
    }
  )
)

V1JobSpec$from_list <- function(x) {
  V1JobSpec$new(
    `active_deadline_seconds` = x[["activeDeadlineSeconds"]],
    `backoff_limit` = x[["backoffLimit"]],
    `backoff_limit_per_index` = x[["backoffLimitPerIndex"]],
    `completion_mode` = x[["completionMode"]],
    `completions` = x[["completions"]],
    `managed_by` = x[["managedBy"]],
    `manual_selector` = x[["manualSelector"]],
    `max_failed_indexes` = x[["maxFailedIndexes"]],
    `parallelism` = x[["parallelism"]],
    `pod_failure_policy` = if (is.null(x[["podFailurePolicy"]])) NULL else V1PodFailurePolicy$from_list(x[["podFailurePolicy"]]),
    `pod_replacement_policy` = x[["podReplacementPolicy"]],
    `selector` = if (is.null(x[["selector"]])) NULL else V1LabelSelector$from_list(x[["selector"]]),
    `success_policy` = if (is.null(x[["successPolicy"]])) NULL else V1SuccessPolicy$from_list(x[["successPolicy"]]),
    `suspend` = x[["suspend"]],
    `template` = if (is.null(x[["template"]])) NULL else V1PodTemplateSpec$from_list(x[["template"]]),
    `ttl_seconds_after_finished` = x[["ttlSecondsAfterFinished"]]
  )
}

V1JobSpec$required_fields <- c("template")

