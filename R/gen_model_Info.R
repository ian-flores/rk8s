# Generated by tools/gen/generate.R -- do not edit by hand.

#' Info
#'
#' Info contains versioning information. how we'll want to distribute that information.
#' @name Info
#' @noMd
#' @export
Info <- R6::R6Class(
  "Info",
  public = list(
    `build_date` = NULL,
    `compiler` = NULL,
    `git_commit` = NULL,
    `git_tree_state` = NULL,
    `git_version` = NULL,
    `go_version` = NULL,
    `major` = NULL,
    `minor` = NULL,
    `platform` = NULL
    ,
    #' @description Construct.
    initialize = function(`build_date` = NULL, `compiler` = NULL, `git_commit` = NULL, `git_tree_state` = NULL, `git_version` = NULL, `go_version` = NULL, `major` = NULL, `minor` = NULL, `platform` = NULL) {
      self$`build_date` <- `build_date`
      self$`compiler` <- `compiler`
      self$`git_commit` <- `git_commit`
      self$`git_tree_state` <- `git_tree_state`
      self$`git_version` <- `git_version`
      self$`go_version` <- `go_version`
      self$`major` <- `major`
      self$`minor` <- `minor`
      self$`platform` <- `platform`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["buildDate"]] <- self$`build_date`
      out[["compiler"]] <- self$`compiler`
      out[["gitCommit"]] <- self$`git_commit`
      out[["gitTreeState"]] <- self$`git_tree_state`
      out[["gitVersion"]] <- self$`git_version`
      out[["goVersion"]] <- self$`go_version`
      out[["major"]] <- self$`major`
      out[["minor"]] <- self$`minor`
      out[["platform"]] <- self$`platform`
      drop_nulls_deep(out)
    }
  )
)

Info$from_list <- function(x) {
  Info$new(
    `build_date` = x[["buildDate"]],
    `compiler` = x[["compiler"]],
    `git_commit` = x[["gitCommit"]],
    `git_tree_state` = x[["gitTreeState"]],
    `git_version` = x[["gitVersion"]],
    `go_version` = x[["goVersion"]],
    `major` = x[["major"]],
    `minor` = x[["minor"]],
    `platform` = x[["platform"]]
  )
}

Info$required_fields <- c("major", "minor", "gitVersion", "gitCommit", "gitTreeState", "buildDate", "goVersion", "compiler", "platform")

