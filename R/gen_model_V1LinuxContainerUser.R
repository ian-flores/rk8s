# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1LinuxContainerUser
#'
#' LinuxContainerUser represents user identity information in Linux containers
#' @name V1LinuxContainerUser
#' @noMd
#' @export
V1LinuxContainerUser <- R6::R6Class(
  "V1LinuxContainerUser",
  public = list(
    `gid` = NULL,
    `supplemental_groups` = NULL,
    `uid` = NULL
    ,
    #' @description Construct.
    initialize = function(`gid` = NULL, `supplemental_groups` = NULL, `uid` = NULL) {
      self$`gid` <- `gid`
      self$`supplemental_groups` <- `supplemental_groups`
      self$`uid` <- `uid`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["gid"]] <- self$`gid`
      out[["supplementalGroups"]] <- self$`supplemental_groups`
      out[["uid"]] <- self$`uid`
      drop_nulls_deep(out)
    }
  )
)

V1LinuxContainerUser$from_list <- function(x) {
  V1LinuxContainerUser$new(
    `gid` = x[["gid"]],
    `supplemental_groups` = x[["supplementalGroups"]],
    `uid` = x[["uid"]]
  )
}

V1LinuxContainerUser$required_fields <- c("uid", "gid")

