# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1ResourceQuotaStatus
#'
#' ResourceQuotaStatus defines the enforced hard limits and observed use.
#' @name V1ResourceQuotaStatus
#' @noMd
#' @export
V1ResourceQuotaStatus <- R6::R6Class(
  "V1ResourceQuotaStatus",
  public = list(
    `hard` = NULL,
    `used` = NULL
    ,
    #' @description Construct.
    initialize = function(`hard` = NULL, `used` = NULL) {
      self$`hard` <- `hard`
      self$`used` <- `used`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["hard"]] <- if (!is.null(self$`hard`)) lapply(self$`hard`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["used"]] <- if (!is.null(self$`used`)) lapply(self$`used`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1ResourceQuotaStatus$from_list <- function(x) {
  V1ResourceQuotaStatus$new(
    `hard` = if (is.null(x[["hard"]])) NULL else lapply(x[["hard"]], Quantity$from_list),
    `used` = if (is.null(x[["used"]])) NULL else lapply(x[["used"]], Quantity$from_list)
  )
}

