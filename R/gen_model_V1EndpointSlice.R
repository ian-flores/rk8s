# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1EndpointSlice
#'
#' EndpointSlice represents a subset of the endpoints that implement a service. For a given service there may be multiple EndpointSlice objects, selected by labels, which must be joined to produce the full set of endpoints.
#' @name V1EndpointSlice
#' @noMd
#' @export
V1EndpointSlice <- R6::R6Class(
  "V1EndpointSlice",
  public = list(
    `address_type` = NULL,
    `api_version` = NULL,
    `endpoints` = NULL,
    `kind` = NULL,
    `metadata` = NULL,
    `ports` = NULL
    ,
    #' @description Construct.
    initialize = function(`address_type` = NULL, `api_version` = NULL, `endpoints` = NULL, `kind` = NULL, `metadata` = NULL, `ports` = NULL) {
      self$`address_type` <- `address_type`
      self$`api_version` <- `api_version`
      self$`endpoints` <- `endpoints`
      self$`kind` <- `kind`
      self$`metadata` <- `metadata`
      self$`ports` <- `ports`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["addressType"]] <- self$`address_type`
      out[["apiVersion"]] <- self$`api_version`
      out[["endpoints"]] <- if (!is.null(self$`endpoints`)) lapply(self$`endpoints`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["kind"]] <- self$`kind`
      out[["metadata"]] <- if (!is.null(self$`metadata`)) self$`metadata`$to_list() else NULL
      out[["ports"]] <- if (!is.null(self$`ports`)) lapply(self$`ports`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1EndpointSlice$from_list <- function(x) {
  V1EndpointSlice$new(
    `address_type` = x[["addressType"]],
    `api_version` = x[["apiVersion"]],
    `endpoints` = if (is.null(x[["endpoints"]])) NULL else lapply(x[["endpoints"]], V1Endpoint$from_list),
    `kind` = x[["kind"]],
    `metadata` = if (is.null(x[["metadata"]])) NULL else V1ObjectMeta$from_list(x[["metadata"]]),
    `ports` = if (is.null(x[["ports"]])) NULL else lapply(x[["ports"]], DiscoveryV1EndpointPort$from_list)
  )
}

V1EndpointSlice$required_fields <- c("addressType", "endpoints")

