# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1NodeStatus
#'
#' NodeStatus is information about the current status of a node.
#' @name V1NodeStatus
#' @noMd
#' @export
V1NodeStatus <- R6::R6Class(
  "V1NodeStatus",
  public = list(
    `addresses` = NULL,
    `allocatable` = NULL,
    `capacity` = NULL,
    `conditions` = NULL,
    `config` = NULL,
    `daemon_endpoints` = NULL,
    `features` = NULL,
    `images` = NULL,
    `node_info` = NULL,
    `phase` = NULL,
    `runtime_handlers` = NULL,
    `volumes_attached` = NULL,
    `volumes_in_use` = NULL
    ,
    #' @description Construct.
    initialize = function(`addresses` = NULL, `allocatable` = NULL, `capacity` = NULL, `conditions` = NULL, `config` = NULL, `daemon_endpoints` = NULL, `features` = NULL, `images` = NULL, `node_info` = NULL, `phase` = NULL, `runtime_handlers` = NULL, `volumes_attached` = NULL, `volumes_in_use` = NULL) {
      self$`addresses` <- `addresses`
      self$`allocatable` <- `allocatable`
      self$`capacity` <- `capacity`
      self$`conditions` <- `conditions`
      self$`config` <- `config`
      self$`daemon_endpoints` <- `daemon_endpoints`
      self$`features` <- `features`
      self$`images` <- `images`
      self$`node_info` <- `node_info`
      self$`phase` <- `phase`
      self$`runtime_handlers` <- `runtime_handlers`
      self$`volumes_attached` <- `volumes_attached`
      self$`volumes_in_use` <- `volumes_in_use`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["addresses"]] <- if (!is.null(self$`addresses`)) lapply(self$`addresses`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["allocatable"]] <- if (!is.null(self$`allocatable`)) lapply(self$`allocatable`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["capacity"]] <- if (!is.null(self$`capacity`)) lapply(self$`capacity`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["conditions"]] <- if (!is.null(self$`conditions`)) lapply(self$`conditions`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["config"]] <- if (!is.null(self$`config`)) self$`config`$to_list() else NULL
      out[["daemonEndpoints"]] <- if (!is.null(self$`daemon_endpoints`)) self$`daemon_endpoints`$to_list() else NULL
      out[["features"]] <- if (!is.null(self$`features`)) self$`features`$to_list() else NULL
      out[["images"]] <- if (!is.null(self$`images`)) lapply(self$`images`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["nodeInfo"]] <- if (!is.null(self$`node_info`)) self$`node_info`$to_list() else NULL
      out[["phase"]] <- self$`phase`
      out[["runtimeHandlers"]] <- if (!is.null(self$`runtime_handlers`)) lapply(self$`runtime_handlers`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["volumesAttached"]] <- if (!is.null(self$`volumes_attached`)) lapply(self$`volumes_attached`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["volumesInUse"]] <- self$`volumes_in_use`
      drop_nulls_deep(out)
    }
  )
)

V1NodeStatus$from_list <- function(x) {
  V1NodeStatus$new(
    `addresses` = if (is.null(x[["addresses"]])) NULL else lapply(x[["addresses"]], V1NodeAddress$from_list),
    `allocatable` = if (is.null(x[["allocatable"]])) NULL else lapply(x[["allocatable"]], Quantity$from_list),
    `capacity` = if (is.null(x[["capacity"]])) NULL else lapply(x[["capacity"]], Quantity$from_list),
    `conditions` = if (is.null(x[["conditions"]])) NULL else lapply(x[["conditions"]], V1NodeCondition$from_list),
    `config` = if (is.null(x[["config"]])) NULL else V1NodeConfigStatus$from_list(x[["config"]]),
    `daemon_endpoints` = if (is.null(x[["daemonEndpoints"]])) NULL else V1NodeDaemonEndpoints$from_list(x[["daemonEndpoints"]]),
    `features` = if (is.null(x[["features"]])) NULL else V1NodeFeatures$from_list(x[["features"]]),
    `images` = if (is.null(x[["images"]])) NULL else lapply(x[["images"]], V1ContainerImage$from_list),
    `node_info` = if (is.null(x[["nodeInfo"]])) NULL else V1NodeSystemInfo$from_list(x[["nodeInfo"]]),
    `phase` = x[["phase"]],
    `runtime_handlers` = if (is.null(x[["runtimeHandlers"]])) NULL else lapply(x[["runtimeHandlers"]], V1NodeRuntimeHandler$from_list),
    `volumes_attached` = if (is.null(x[["volumesAttached"]])) NULL else lapply(x[["volumesAttached"]], V1AttachedVolume$from_list),
    `volumes_in_use` = x[["volumesInUse"]]
  )
}

