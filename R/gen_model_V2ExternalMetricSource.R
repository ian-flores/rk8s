# Generated by tools/gen/generate.R -- do not edit by hand.

#' V2ExternalMetricSource
#'
#' ExternalMetricSource indicates how to scale on a metric not associated with any Kubernetes object (for example length of queue in cloud messaging service, or QPS from loadbalancer running outside of cluster).
#' @name V2ExternalMetricSource
#' @noMd
#' @export
V2ExternalMetricSource <- R6::R6Class(
  "V2ExternalMetricSource",
  public = list(
    `metric` = NULL,
    `target` = NULL
    ,
    #' @description Construct.
    initialize = function(`metric` = NULL, `target` = NULL) {
      self$`metric` <- `metric`
      self$`target` <- `target`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["metric"]] <- if (!is.null(self$`metric`)) self$`metric`$to_list() else NULL
      out[["target"]] <- if (!is.null(self$`target`)) self$`target`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V2ExternalMetricSource$from_list <- function(x) {
  V2ExternalMetricSource$new(
    `metric` = if (is.null(x[["metric"]])) NULL else V2MetricIdentifier$from_list(x[["metric"]]),
    `target` = if (is.null(x[["target"]])) NULL else V2MetricTarget$from_list(x[["target"]])
  )
}

V2ExternalMetricSource$required_fields <- c("metric", "target")

