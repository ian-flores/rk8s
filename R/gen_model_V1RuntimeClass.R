# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1RuntimeClass
#'
#' RuntimeClass defines a class of container runtime supported in the cluster. The RuntimeClass is used to determine which container runtime is used to run all containers in a pod. RuntimeClasses are manually defined by a user or cluster provisioner, and referenced in the PodSpec. The Kubelet is responsible for resolving the RuntimeClassName reference before running the pod.  For more details, see https://kubernetes.io/docs/concepts/containers/runtime-class/
#' @name V1RuntimeClass
#' @noMd
#' @export
V1RuntimeClass <- R6::R6Class(
  "V1RuntimeClass",
  public = list(
    `api_version` = NULL,
    `handler` = NULL,
    `kind` = NULL,
    `metadata` = NULL,
    `overhead` = NULL,
    `scheduling` = NULL
    ,
    #' @description Construct.
    initialize = function(`api_version` = NULL, `handler` = NULL, `kind` = NULL, `metadata` = NULL, `overhead` = NULL, `scheduling` = NULL) {
      self$`api_version` <- `api_version`
      self$`handler` <- `handler`
      self$`kind` <- `kind`
      self$`metadata` <- `metadata`
      self$`overhead` <- `overhead`
      self$`scheduling` <- `scheduling`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["apiVersion"]] <- self$`api_version`
      out[["handler"]] <- self$`handler`
      out[["kind"]] <- self$`kind`
      out[["metadata"]] <- if (!is.null(self$`metadata`)) self$`metadata`$to_list() else NULL
      out[["overhead"]] <- if (!is.null(self$`overhead`)) self$`overhead`$to_list() else NULL
      out[["scheduling"]] <- if (!is.null(self$`scheduling`)) self$`scheduling`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V1RuntimeClass$from_list <- function(x) {
  V1RuntimeClass$new(
    `api_version` = x[["apiVersion"]],
    `handler` = x[["handler"]],
    `kind` = x[["kind"]],
    `metadata` = if (is.null(x[["metadata"]])) NULL else V1ObjectMeta$from_list(x[["metadata"]]),
    `overhead` = if (is.null(x[["overhead"]])) NULL else V1Overhead$from_list(x[["overhead"]]),
    `scheduling` = if (is.null(x[["scheduling"]])) NULL else V1Scheduling$from_list(x[["scheduling"]])
  )
}

V1RuntimeClass$required_fields <- c("handler")

