# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1PodDNSConfig
#'
#' PodDNSConfig defines the DNS parameters of a pod in addition to those generated from DNSPolicy.
#' @name V1PodDNSConfig
#' @noMd
#' @export
V1PodDNSConfig <- R6::R6Class(
  "V1PodDNSConfig",
  public = list(
    `nameservers` = NULL,
    `options` = NULL,
    `searches` = NULL
    ,
    #' @description Construct.
    initialize = function(`nameservers` = NULL, `options` = NULL, `searches` = NULL) {
      self$`nameservers` <- `nameservers`
      self$`options` <- `options`
      self$`searches` <- `searches`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["nameservers"]] <- self$`nameservers`
      out[["options"]] <- if (!is.null(self$`options`)) lapply(self$`options`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["searches"]] <- self$`searches`
      drop_nulls_deep(out)
    }
  )
)

V1PodDNSConfig$from_list <- function(x) {
  V1PodDNSConfig$new(
    `nameservers` = x[["nameservers"]],
    `options` = if (is.null(x[["options"]])) NULL else lapply(x[["options"]], V1PodDNSConfigOption$from_list),
    `searches` = x[["searches"]]
  )
}

