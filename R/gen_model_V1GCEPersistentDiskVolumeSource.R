# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1GCEPersistentDiskVolumeSource
#'
#' Represents a Persistent Disk resource in Google Compute Engine.
#' 
#' A GCE PD must exist before mounting to a container. The disk must also be in the same GCE project and zone as the kubelet. A GCE PD can only be mounted as read/write once or read-only many times. GCE PDs support ownership management and SELinux relabeling.
#' @name V1GCEPersistentDiskVolumeSource
#' @noMd
#' @export
V1GCEPersistentDiskVolumeSource <- R6::R6Class(
  "V1GCEPersistentDiskVolumeSource",
  public = list(
    `fs_type` = NULL,
    `partition` = NULL,
    `pd_name` = NULL,
    `read_only` = NULL
    ,
    #' @description Construct.
    initialize = function(`fs_type` = NULL, `partition` = NULL, `pd_name` = NULL, `read_only` = NULL) {
      self$`fs_type` <- `fs_type`
      self$`partition` <- `partition`
      self$`pd_name` <- `pd_name`
      self$`read_only` <- `read_only`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["fsType"]] <- self$`fs_type`
      out[["partition"]] <- self$`partition`
      out[["pdName"]] <- self$`pd_name`
      out[["readOnly"]] <- self$`read_only`
      drop_nulls_deep(out)
    }
  )
)

V1GCEPersistentDiskVolumeSource$from_list <- function(x) {
  V1GCEPersistentDiskVolumeSource$new(
    `fs_type` = x[["fsType"]],
    `partition` = x[["partition"]],
    `pd_name` = x[["pdName"]],
    `read_only` = x[["readOnly"]]
  )
}

V1GCEPersistentDiskVolumeSource$required_fields <- c("pdName")

