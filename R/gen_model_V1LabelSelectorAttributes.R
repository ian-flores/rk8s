# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1LabelSelectorAttributes
#'
#' LabelSelectorAttributes indicates a label limited access. Webhook authors are encouraged to * ensure rawSelector and requirements are not both set * consider the requirements field if set * not try to parse or consider the rawSelector field if set. This is to avoid another CVE-2022-2880 (i.e. getting different systems to agree on how exactly to parse a query is not something we want), see https://www.oxeye.io/resources/golang-parameter-smuggling-attack for more details. For the *SubjectAccessReview endpoints of the kube-apiserver: * If rawSelector is empty and requirements are empty, the request is not limited. * If rawSelector is present and requirements are empty, the rawSelector will be parsed and limited if the parsing succeeds. * If rawSelector is empty and requirements are present, the requirements should be honored * If rawSelector is present and requirements are present, the request is invalid.
#' @name V1LabelSelectorAttributes
#' @noMd
#' @export
V1LabelSelectorAttributes <- R6::R6Class(
  "V1LabelSelectorAttributes",
  public = list(
    `raw_selector` = NULL,
    `requirements` = NULL
    ,
    #' @description Construct.
    initialize = function(`raw_selector` = NULL, `requirements` = NULL) {
      self$`raw_selector` <- `raw_selector`
      self$`requirements` <- `requirements`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["rawSelector"]] <- self$`raw_selector`
      out[["requirements"]] <- if (!is.null(self$`requirements`)) lapply(self$`requirements`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1LabelSelectorAttributes$from_list <- function(x) {
  V1LabelSelectorAttributes$new(
    `raw_selector` = x[["rawSelector"]],
    `requirements` = if (is.null(x[["requirements"]])) NULL else lapply(x[["requirements"]], V1LabelSelectorRequirement$from_list)
  )
}

