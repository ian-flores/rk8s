# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1RollingUpdateDeployment
#'
#' Spec to control the desired behavior of rolling update.
#' @name V1RollingUpdateDeployment
#' @noMd
#' @export
V1RollingUpdateDeployment <- R6::R6Class(
  "V1RollingUpdateDeployment",
  public = list(
    `max_surge` = NULL,
    `max_unavailable` = NULL
    ,
    #' @description Construct.
    initialize = function(`max_surge` = NULL, `max_unavailable` = NULL) {
      self$`max_surge` <- `max_surge`
      self$`max_unavailable` <- `max_unavailable`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["maxSurge"]] <- if (!is.null(self$`max_surge`)) self$`max_surge`$to_list() else NULL
      out[["maxUnavailable"]] <- if (!is.null(self$`max_unavailable`)) self$`max_unavailable`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V1RollingUpdateDeployment$from_list <- function(x) {
  V1RollingUpdateDeployment$new(
    `max_surge` = if (is.null(x[["maxSurge"]])) NULL else IntOrString$from_list(x[["maxSurge"]]),
    `max_unavailable` = if (is.null(x[["maxUnavailable"]])) NULL else IntOrString$from_list(x[["maxUnavailable"]])
  )
}

