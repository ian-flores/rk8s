# Generated by tools/gen/generate.R -- do not edit by hand.

#' V2ContainerResourceMetricSource
#'
#' ContainerResourceMetricSource indicates how to scale on a resource metric known to Kubernetes, as specified in requests and limits, describing each pod in the current scale target (e.g. CPU or memory).  The values will be averaged together before being compared to the target.  Such metrics are built in to Kubernetes, and have special scaling options on top of those available to normal per-pod metrics using the "pods" source.  Only one "target" type should be set.
#' @name V2ContainerResourceMetricSource
#' @noMd
#' @export
V2ContainerResourceMetricSource <- R6::R6Class(
  "V2ContainerResourceMetricSource",
  public = list(
    `container` = NULL,
    `name` = NULL,
    `target` = NULL
    ,
    #' @description Construct.
    initialize = function(`container` = NULL, `name` = NULL, `target` = NULL) {
      self$`container` <- `container`
      self$`name` <- `name`
      self$`target` <- `target`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["container"]] <- self$`container`
      out[["name"]] <- self$`name`
      out[["target"]] <- if (!is.null(self$`target`)) self$`target`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V2ContainerResourceMetricSource$from_list <- function(x) {
  V2ContainerResourceMetricSource$new(
    `container` = x[["container"]],
    `name` = x[["name"]],
    `target` = if (is.null(x[["target"]])) NULL else V2MetricTarget$from_list(x[["target"]])
  )
}

V2ContainerResourceMetricSource$required_fields <- c("name", "target", "container")

