# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1alpha3DeviceClass
#'
#' DeviceClass is a vendor- or admin-provided resource that contains device configuration and selectors. It can be referenced in the device requests of a claim to apply these presets. Cluster scoped.
#' 
#' This is an alpha type and requires enabling the DynamicResourceAllocation feature gate.
#' @name V1alpha3DeviceClass
#' @noMd
#' @export
V1alpha3DeviceClass <- R6::R6Class(
  "V1alpha3DeviceClass",
  public = list(
    `api_version` = NULL,
    `kind` = NULL,
    `metadata` = NULL,
    `spec` = NULL
    ,
    #' @description Construct.
    initialize = function(`api_version` = NULL, `kind` = NULL, `metadata` = NULL, `spec` = NULL) {
      self$`api_version` <- `api_version`
      self$`kind` <- `kind`
      self$`metadata` <- `metadata`
      self$`spec` <- `spec`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["apiVersion"]] <- self$`api_version`
      out[["kind"]] <- self$`kind`
      out[["metadata"]] <- if (!is.null(self$`metadata`)) self$`metadata`$to_list() else NULL
      out[["spec"]] <- if (!is.null(self$`spec`)) self$`spec`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V1alpha3DeviceClass$from_list <- function(x) {
  V1alpha3DeviceClass$new(
    `api_version` = x[["apiVersion"]],
    `kind` = x[["kind"]],
    `metadata` = if (is.null(x[["metadata"]])) NULL else V1ObjectMeta$from_list(x[["metadata"]]),
    `spec` = if (is.null(x[["spec"]])) NULL else V1alpha3DeviceClassSpec$from_list(x[["spec"]])
  )
}

V1alpha3DeviceClass$required_fields <- c("spec")

