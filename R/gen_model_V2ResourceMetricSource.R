# Generated by tools/gen/generate.R -- do not edit by hand.

#' V2ResourceMetricSource
#'
#' ResourceMetricSource indicates how to scale on a resource metric known to Kubernetes, as specified in requests and limits, describing each pod in the current scale target (e.g. CPU or memory).  The values will be averaged together before being compared to the target.  Such metrics are built in to Kubernetes, and have special scaling options on top of those available to normal per-pod metrics using the "pods" source.  Only one "target" type should be set.
#' @name V2ResourceMetricSource
#' @noMd
#' @export
V2ResourceMetricSource <- R6::R6Class(
  "V2ResourceMetricSource",
  public = list(
    `name` = NULL,
    `target` = NULL
    ,
    #' @description Construct.
    initialize = function(`name` = NULL, `target` = NULL) {
      self$`name` <- `name`
      self$`target` <- `target`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["name"]] <- self$`name`
      out[["target"]] <- if (!is.null(self$`target`)) self$`target`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V2ResourceMetricSource$from_list <- function(x) {
  V2ResourceMetricSource$new(
    `name` = x[["name"]],
    `target` = if (is.null(x[["target"]])) NULL else V2MetricTarget$from_list(x[["target"]])
  )
}

V2ResourceMetricSource$required_fields <- c("name", "target")

