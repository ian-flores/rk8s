# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1AggregationRule
#'
#' AggregationRule describes how to locate ClusterRoles to aggregate into the ClusterRole
#' @name V1AggregationRule
#' @noMd
#' @export
V1AggregationRule <- R6::R6Class(
  "V1AggregationRule",
  public = list(
    `cluster_role_selectors` = NULL
    ,
    #' @description Construct.
    initialize = function(`cluster_role_selectors` = NULL) {
      self$`cluster_role_selectors` <- `cluster_role_selectors`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["clusterRoleSelectors"]] <- if (!is.null(self$`cluster_role_selectors`)) lapply(self$`cluster_role_selectors`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1AggregationRule$from_list <- function(x) {
  V1AggregationRule$new(
    `cluster_role_selectors` = if (is.null(x[["clusterRoleSelectors"]])) NULL else lapply(x[["clusterRoleSelectors"]], V1LabelSelector$from_list)
  )
}

