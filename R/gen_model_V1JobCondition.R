# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1JobCondition
#'
#' JobCondition describes current state of a job.
#' @name V1JobCondition
#' @noMd
#' @export
V1JobCondition <- R6::R6Class(
  "V1JobCondition",
  public = list(
    `last_probe_time` = NULL,
    `last_transition_time` = NULL,
    `message` = NULL,
    `reason` = NULL,
    `status` = NULL,
    `type` = NULL
    ,
    #' @description Construct.
    initialize = function(`last_probe_time` = NULL, `last_transition_time` = NULL, `message` = NULL, `reason` = NULL, `status` = NULL, `type` = NULL) {
      self$`last_probe_time` <- `last_probe_time`
      self$`last_transition_time` <- `last_transition_time`
      self$`message` <- `message`
      self$`reason` <- `reason`
      self$`status` <- `status`
      self$`type` <- `type`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["lastProbeTime"]] <- if (!is.null(self$`last_probe_time`)) self$`last_probe_time`$to_list() else NULL
      out[["lastTransitionTime"]] <- if (!is.null(self$`last_transition_time`)) self$`last_transition_time`$to_list() else NULL
      out[["message"]] <- self$`message`
      out[["reason"]] <- self$`reason`
      out[["status"]] <- self$`status`
      out[["type"]] <- self$`type`
      drop_nulls_deep(out)
    }
  )
)

V1JobCondition$from_list <- function(x) {
  V1JobCondition$new(
    `last_probe_time` = if (is.null(x[["lastProbeTime"]])) NULL else V1Time$from_list(x[["lastProbeTime"]]),
    `last_transition_time` = if (is.null(x[["lastTransitionTime"]])) NULL else V1Time$from_list(x[["lastTransitionTime"]]),
    `message` = x[["message"]],
    `reason` = x[["reason"]],
    `status` = x[["status"]],
    `type` = x[["type"]]
  )
}

V1JobCondition$required_fields <- c("type", "status")

