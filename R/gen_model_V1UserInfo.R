# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1UserInfo
#'
#' UserInfo holds the information about the user needed to implement the user.Info interface.
#' @name V1UserInfo
#' @noMd
#' @export
V1UserInfo <- R6::R6Class(
  "V1UserInfo",
  public = list(
    `extra` = NULL,
    `groups` = NULL,
    `uid` = NULL,
    `username` = NULL
    ,
    #' @description Construct.
    initialize = function(`extra` = NULL, `groups` = NULL, `uid` = NULL, `username` = NULL) {
      self$`extra` <- `extra`
      self$`groups` <- `groups`
      self$`uid` <- `uid`
      self$`username` <- `username`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["extra"]] <- self$`extra`
      out[["groups"]] <- self$`groups`
      out[["uid"]] <- self$`uid`
      out[["username"]] <- self$`username`
      drop_nulls_deep(out)
    }
  )
)

V1UserInfo$from_list <- function(x) {
  V1UserInfo$new(
    `extra` = x[["extra"]],
    `groups` = x[["groups"]],
    `uid` = x[["uid"]],
    `username` = x[["username"]]
  )
}

