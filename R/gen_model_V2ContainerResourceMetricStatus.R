# Generated by tools/gen/generate.R -- do not edit by hand.

#' V2ContainerResourceMetricStatus
#'
#' ContainerResourceMetricStatus indicates the current value of a resource metric known to Kubernetes, as specified in requests and limits, describing a single container in each pod in the current scale target (e.g. CPU or memory).  Such metrics are built in to Kubernetes, and have special scaling options on top of those available to normal per-pod metrics using the "pods" source.
#' @name V2ContainerResourceMetricStatus
#' @noMd
#' @export
V2ContainerResourceMetricStatus <- R6::R6Class(
  "V2ContainerResourceMetricStatus",
  public = list(
    `container` = NULL,
    `current` = NULL,
    `name` = NULL
    ,
    #' @description Construct.
    initialize = function(`container` = NULL, `current` = NULL, `name` = NULL) {
      self$`container` <- `container`
      self$`current` <- `current`
      self$`name` <- `name`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["container"]] <- self$`container`
      out[["current"]] <- if (!is.null(self$`current`)) self$`current`$to_list() else NULL
      out[["name"]] <- self$`name`
      drop_nulls_deep(out)
    }
  )
)

V2ContainerResourceMetricStatus$from_list <- function(x) {
  V2ContainerResourceMetricStatus$new(
    `container` = x[["container"]],
    `current` = if (is.null(x[["current"]])) NULL else V2MetricValueStatus$from_list(x[["current"]]),
    `name` = x[["name"]]
  )
}

V2ContainerResourceMetricStatus$required_fields <- c("name", "current", "container")

