# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1Endpoints
#'
#' Endpoints is a collection of endpoints that implement the actual service. Example:
#' 
#' 	 Name: "mysvc",
#' 	 Subsets: 
#' 	   {
#' 	     Addresses: [{"ip": "10.10.1.1"}, {"ip": "10.10.2.2"},
#' 	     Ports: {"name": "a", "port": 8675}, {"name": "b", "port": 309}
#' 	   },
#' 	   {
#' 	     Addresses: {"ip": "10.10.3.3"},
#' 	     Ports: {"name": "a", "port": 93}, {"name": "b", "port": 76}
#' 	   },
#' 	]
#' @name V1Endpoints
#' @noMd
#' @export
V1Endpoints <- R6::R6Class(
  "V1Endpoints",
  public = list(
    `api_version` = NULL,
    `kind` = NULL,
    `metadata` = NULL,
    `subsets` = NULL
    ,
    #' @description Construct.
    initialize = function(`api_version` = NULL, `kind` = NULL, `metadata` = NULL, `subsets` = NULL) {
      self$`api_version` <- `api_version`
      self$`kind` <- `kind`
      self$`metadata` <- `metadata`
      self$`subsets` <- `subsets`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["apiVersion"]] <- self$`api_version`
      out[["kind"]] <- self$`kind`
      out[["metadata"]] <- if (!is.null(self$`metadata`)) self$`metadata`$to_list() else NULL
      out[["subsets"]] <- if (!is.null(self$`subsets`)) lapply(self$`subsets`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1Endpoints$from_list <- function(x) {
  V1Endpoints$new(
    `api_version` = x[["apiVersion"]],
    `kind` = x[["kind"]],
    `metadata` = if (is.null(x[["metadata"]])) NULL else V1ObjectMeta$from_list(x[["metadata"]]),
    `subsets` = if (is.null(x[["subsets"]])) NULL else lapply(x[["subsets"]], V1EndpointSubset$from_list)
  )
}

