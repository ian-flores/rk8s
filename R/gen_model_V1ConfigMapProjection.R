# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1ConfigMapProjection
#'
#' Adapts a ConfigMap into a projected volume.
#' 
#' The contents of the target ConfigMap's Data field will be presented in a projected volume as files using the keys in the Data field as the file names, unless the items element is populated with specific mappings of keys to paths. Note that this is identical to a configmap volume source without the default mode.
#' @name V1ConfigMapProjection
#' @noMd
#' @export
V1ConfigMapProjection <- R6::R6Class(
  "V1ConfigMapProjection",
  public = list(
    `items` = NULL,
    `name` = NULL,
    `optional` = NULL
    ,
    #' @description Construct.
    initialize = function(`items` = NULL, `name` = NULL, `optional` = NULL) {
      self$`items` <- `items`
      self$`name` <- `name`
      self$`optional` <- `optional`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["items"]] <- if (!is.null(self$`items`)) lapply(self$`items`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["name"]] <- self$`name`
      out[["optional"]] <- self$`optional`
      drop_nulls_deep(out)
    }
  )
)

V1ConfigMapProjection$from_list <- function(x) {
  V1ConfigMapProjection$new(
    `items` = if (is.null(x[["items"]])) NULL else lapply(x[["items"]], V1KeyToPath$from_list),
    `name` = x[["name"]],
    `optional` = x[["optional"]]
  )
}

