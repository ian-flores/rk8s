# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1ReplicaSetStatus
#'
#' ReplicaSetStatus represents the current status of a ReplicaSet.
#' @name V1ReplicaSetStatus
#' @noMd
#' @export
V1ReplicaSetStatus <- R6::R6Class(
  "V1ReplicaSetStatus",
  public = list(
    `available_replicas` = NULL,
    `conditions` = NULL,
    `fully_labeled_replicas` = NULL,
    `observed_generation` = NULL,
    `ready_replicas` = NULL,
    `replicas` = NULL
    ,
    #' @description Construct.
    initialize = function(`available_replicas` = NULL, `conditions` = NULL, `fully_labeled_replicas` = NULL, `observed_generation` = NULL, `ready_replicas` = NULL, `replicas` = NULL) {
      self$`available_replicas` <- `available_replicas`
      self$`conditions` <- `conditions`
      self$`fully_labeled_replicas` <- `fully_labeled_replicas`
      self$`observed_generation` <- `observed_generation`
      self$`ready_replicas` <- `ready_replicas`
      self$`replicas` <- `replicas`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["availableReplicas"]] <- self$`available_replicas`
      out[["conditions"]] <- if (!is.null(self$`conditions`)) lapply(self$`conditions`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["fullyLabeledReplicas"]] <- self$`fully_labeled_replicas`
      out[["observedGeneration"]] <- self$`observed_generation`
      out[["readyReplicas"]] <- self$`ready_replicas`
      out[["replicas"]] <- self$`replicas`
      drop_nulls_deep(out)
    }
  )
)

V1ReplicaSetStatus$from_list <- function(x) {
  V1ReplicaSetStatus$new(
    `available_replicas` = x[["availableReplicas"]],
    `conditions` = if (is.null(x[["conditions"]])) NULL else lapply(x[["conditions"]], V1ReplicaSetCondition$from_list),
    `fully_labeled_replicas` = x[["fullyLabeledReplicas"]],
    `observed_generation` = x[["observedGeneration"]],
    `ready_replicas` = x[["readyReplicas"]],
    `replicas` = x[["replicas"]]
  )
}

V1ReplicaSetStatus$required_fields <- c("replicas")

