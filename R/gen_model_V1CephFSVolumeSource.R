# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1CephFSVolumeSource
#'
#' Represents a Ceph Filesystem mount that lasts the lifetime of a pod Cephfs volumes do not support ownership management or SELinux relabeling.
#' @name V1CephFSVolumeSource
#' @noMd
#' @export
V1CephFSVolumeSource <- R6::R6Class(
  "V1CephFSVolumeSource",
  public = list(
    `monitors` = NULL,
    `path` = NULL,
    `read_only` = NULL,
    `secret_file` = NULL,
    `secret_ref` = NULL,
    `user` = NULL
    ,
    #' @description Construct.
    initialize = function(`monitors` = NULL, `path` = NULL, `read_only` = NULL, `secret_file` = NULL, `secret_ref` = NULL, `user` = NULL) {
      self$`monitors` <- `monitors`
      self$`path` <- `path`
      self$`read_only` <- `read_only`
      self$`secret_file` <- `secret_file`
      self$`secret_ref` <- `secret_ref`
      self$`user` <- `user`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["monitors"]] <- self$`monitors`
      out[["path"]] <- self$`path`
      out[["readOnly"]] <- self$`read_only`
      out[["secretFile"]] <- self$`secret_file`
      out[["secretRef"]] <- if (!is.null(self$`secret_ref`)) self$`secret_ref`$to_list() else NULL
      out[["user"]] <- self$`user`
      drop_nulls_deep(out)
    }
  )
)

V1CephFSVolumeSource$from_list <- function(x) {
  V1CephFSVolumeSource$new(
    `monitors` = x[["monitors"]],
    `path` = x[["path"]],
    `read_only` = x[["readOnly"]],
    `secret_file` = x[["secretFile"]],
    `secret_ref` = if (is.null(x[["secretRef"]])) NULL else V1LocalObjectReference$from_list(x[["secretRef"]]),
    `user` = x[["user"]]
  )
}

V1CephFSVolumeSource$required_fields <- c("monitors")

