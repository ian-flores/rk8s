# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1LeaseSpec
#'
#' LeaseSpec is a specification of a Lease.
#' @name V1LeaseSpec
#' @noMd
#' @export
V1LeaseSpec <- R6::R6Class(
  "V1LeaseSpec",
  public = list(
    `acquire_time` = NULL,
    `holder_identity` = NULL,
    `lease_duration_seconds` = NULL,
    `lease_transitions` = NULL,
    `preferred_holder` = NULL,
    `renew_time` = NULL,
    `strategy` = NULL
    ,
    #' @description Construct.
    initialize = function(`acquire_time` = NULL, `holder_identity` = NULL, `lease_duration_seconds` = NULL, `lease_transitions` = NULL, `preferred_holder` = NULL, `renew_time` = NULL, `strategy` = NULL) {
      self$`acquire_time` <- `acquire_time`
      self$`holder_identity` <- `holder_identity`
      self$`lease_duration_seconds` <- `lease_duration_seconds`
      self$`lease_transitions` <- `lease_transitions`
      self$`preferred_holder` <- `preferred_holder`
      self$`renew_time` <- `renew_time`
      self$`strategy` <- `strategy`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["acquireTime"]] <- if (!is.null(self$`acquire_time`)) self$`acquire_time`$to_list() else NULL
      out[["holderIdentity"]] <- self$`holder_identity`
      out[["leaseDurationSeconds"]] <- self$`lease_duration_seconds`
      out[["leaseTransitions"]] <- self$`lease_transitions`
      out[["preferredHolder"]] <- self$`preferred_holder`
      out[["renewTime"]] <- if (!is.null(self$`renew_time`)) self$`renew_time`$to_list() else NULL
      out[["strategy"]] <- self$`strategy`
      drop_nulls_deep(out)
    }
  )
)

V1LeaseSpec$from_list <- function(x) {
  V1LeaseSpec$new(
    `acquire_time` = if (is.null(x[["acquireTime"]])) NULL else V1MicroTime$from_list(x[["acquireTime"]]),
    `holder_identity` = x[["holderIdentity"]],
    `lease_duration_seconds` = x[["leaseDurationSeconds"]],
    `lease_transitions` = x[["leaseTransitions"]],
    `preferred_holder` = x[["preferredHolder"]],
    `renew_time` = if (is.null(x[["renewTime"]])) NULL else V1MicroTime$from_list(x[["renewTime"]]),
    `strategy` = x[["strategy"]]
  )
}

