# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1RBDVolumeSource
#'
#' Represents a Rados Block Device mount that lasts the lifetime of a pod. RBD volumes support ownership management and SELinux relabeling.
#' @name V1RBDVolumeSource
#' @noMd
#' @export
V1RBDVolumeSource <- R6::R6Class(
  "V1RBDVolumeSource",
  public = list(
    `fs_type` = NULL,
    `image` = NULL,
    `keyring` = NULL,
    `monitors` = NULL,
    `pool` = NULL,
    `read_only` = NULL,
    `secret_ref` = NULL,
    `user` = NULL
    ,
    #' @description Construct.
    initialize = function(`fs_type` = NULL, `image` = NULL, `keyring` = NULL, `monitors` = NULL, `pool` = NULL, `read_only` = NULL, `secret_ref` = NULL, `user` = NULL) {
      self$`fs_type` <- `fs_type`
      self$`image` <- `image`
      self$`keyring` <- `keyring`
      self$`monitors` <- `monitors`
      self$`pool` <- `pool`
      self$`read_only` <- `read_only`
      self$`secret_ref` <- `secret_ref`
      self$`user` <- `user`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["fsType"]] <- self$`fs_type`
      out[["image"]] <- self$`image`
      out[["keyring"]] <- self$`keyring`
      out[["monitors"]] <- self$`monitors`
      out[["pool"]] <- self$`pool`
      out[["readOnly"]] <- self$`read_only`
      out[["secretRef"]] <- if (!is.null(self$`secret_ref`)) self$`secret_ref`$to_list() else NULL
      out[["user"]] <- self$`user`
      drop_nulls_deep(out)
    }
  )
)

V1RBDVolumeSource$from_list <- function(x) {
  V1RBDVolumeSource$new(
    `fs_type` = x[["fsType"]],
    `image` = x[["image"]],
    `keyring` = x[["keyring"]],
    `monitors` = x[["monitors"]],
    `pool` = x[["pool"]],
    `read_only` = x[["readOnly"]],
    `secret_ref` = if (is.null(x[["secretRef"]])) NULL else V1LocalObjectReference$from_list(x[["secretRef"]]),
    `user` = x[["user"]]
  )
}

V1RBDVolumeSource$required_fields <- c("monitors", "image")

