# Generated by tools/gen/generate.R -- do not edit by hand.

#' V2MetricIdentifier
#'
#' MetricIdentifier defines the name and optionally selector for a metric
#' @name V2MetricIdentifier
#' @noMd
#' @export
V2MetricIdentifier <- R6::R6Class(
  "V2MetricIdentifier",
  public = list(
    `name` = NULL,
    `selector` = NULL
    ,
    #' @description Construct.
    initialize = function(`name` = NULL, `selector` = NULL) {
      self$`name` <- `name`
      self$`selector` <- `selector`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["name"]] <- self$`name`
      out[["selector"]] <- if (!is.null(self$`selector`)) self$`selector`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V2MetricIdentifier$from_list <- function(x) {
  V2MetricIdentifier$new(
    `name` = x[["name"]],
    `selector` = if (is.null(x[["selector"]])) NULL else V1LabelSelector$from_list(x[["selector"]])
  )
}

V2MetricIdentifier$required_fields <- c("name")

