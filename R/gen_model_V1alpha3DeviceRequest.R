# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1alpha3DeviceRequest
#'
#' DeviceRequest is a request for devices required for a claim. This is typically a request for a single resource like a device, but can also ask for several identical devices.
#' 
#' A DeviceClassName is currently required. Clients must check that it is indeed set. It's absence indicates that something changed in a way that is not supported by the client yet, in which case it must refuse to handle the request.
#' @name V1alpha3DeviceRequest
#' @noMd
#' @export
V1alpha3DeviceRequest <- R6::R6Class(
  "V1alpha3DeviceRequest",
  public = list(
    `admin_access` = NULL,
    `allocation_mode` = NULL,
    `count` = NULL,
    `device_class_name` = NULL,
    `name` = NULL,
    `selectors` = NULL
    ,
    #' @description Construct.
    initialize = function(`admin_access` = NULL, `allocation_mode` = NULL, `count` = NULL, `device_class_name` = NULL, `name` = NULL, `selectors` = NULL) {
      self$`admin_access` <- `admin_access`
      self$`allocation_mode` <- `allocation_mode`
      self$`count` <- `count`
      self$`device_class_name` <- `device_class_name`
      self$`name` <- `name`
      self$`selectors` <- `selectors`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["adminAccess"]] <- self$`admin_access`
      out[["allocationMode"]] <- self$`allocation_mode`
      out[["count"]] <- self$`count`
      out[["deviceClassName"]] <- self$`device_class_name`
      out[["name"]] <- self$`name`
      out[["selectors"]] <- if (!is.null(self$`selectors`)) lapply(self$`selectors`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1alpha3DeviceRequest$from_list <- function(x) {
  V1alpha3DeviceRequest$new(
    `admin_access` = x[["adminAccess"]],
    `allocation_mode` = x[["allocationMode"]],
    `count` = x[["count"]],
    `device_class_name` = x[["deviceClassName"]],
    `name` = x[["name"]],
    `selectors` = if (is.null(x[["selectors"]])) NULL else lapply(x[["selectors"]], V1alpha3DeviceSelector$from_list)
  )
}

V1alpha3DeviceRequest$required_fields <- c("name", "deviceClassName")

