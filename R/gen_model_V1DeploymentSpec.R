# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1DeploymentSpec
#'
#' DeploymentSpec is the specification of the desired behavior of the Deployment.
#' @name V1DeploymentSpec
#' @noMd
#' @export
V1DeploymentSpec <- R6::R6Class(
  "V1DeploymentSpec",
  public = list(
    `min_ready_seconds` = NULL,
    `paused` = NULL,
    `progress_deadline_seconds` = NULL,
    `replicas` = NULL,
    `revision_history_limit` = NULL,
    `selector` = NULL,
    `strategy` = NULL,
    `template` = NULL
    ,
    #' @description Construct.
    initialize = function(`min_ready_seconds` = NULL, `paused` = NULL, `progress_deadline_seconds` = NULL, `replicas` = NULL, `revision_history_limit` = NULL, `selector` = NULL, `strategy` = NULL, `template` = NULL) {
      self$`min_ready_seconds` <- `min_ready_seconds`
      self$`paused` <- `paused`
      self$`progress_deadline_seconds` <- `progress_deadline_seconds`
      self$`replicas` <- `replicas`
      self$`revision_history_limit` <- `revision_history_limit`
      self$`selector` <- `selector`
      self$`strategy` <- `strategy`
      self$`template` <- `template`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["minReadySeconds"]] <- self$`min_ready_seconds`
      out[["paused"]] <- self$`paused`
      out[["progressDeadlineSeconds"]] <- self$`progress_deadline_seconds`
      out[["replicas"]] <- self$`replicas`
      out[["revisionHistoryLimit"]] <- self$`revision_history_limit`
      out[["selector"]] <- if (!is.null(self$`selector`)) self$`selector`$to_list() else NULL
      out[["strategy"]] <- if (!is.null(self$`strategy`)) self$`strategy`$to_list() else NULL
      out[["template"]] <- if (!is.null(self$`template`)) self$`template`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V1DeploymentSpec$from_list <- function(x) {
  V1DeploymentSpec$new(
    `min_ready_seconds` = x[["minReadySeconds"]],
    `paused` = x[["paused"]],
    `progress_deadline_seconds` = x[["progressDeadlineSeconds"]],
    `replicas` = x[["replicas"]],
    `revision_history_limit` = x[["revisionHistoryLimit"]],
    `selector` = if (is.null(x[["selector"]])) NULL else V1LabelSelector$from_list(x[["selector"]]),
    `strategy` = if (is.null(x[["strategy"]])) NULL else V1DeploymentStrategy$from_list(x[["strategy"]]),
    `template` = if (is.null(x[["template"]])) NULL else V1PodTemplateSpec$from_list(x[["template"]])
  )
}

V1DeploymentSpec$required_fields <- c("selector", "template")

