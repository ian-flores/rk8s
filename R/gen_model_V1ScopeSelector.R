# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1ScopeSelector
#'
#' A scope selector represents the AND of the selectors represented by the scoped-resource selector requirements.
#' @name V1ScopeSelector
#' @noMd
#' @export
V1ScopeSelector <- R6::R6Class(
  "V1ScopeSelector",
  public = list(
    `match_expressions` = NULL
    ,
    #' @description Construct.
    initialize = function(`match_expressions` = NULL) {
      self$`match_expressions` <- `match_expressions`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["matchExpressions"]] <- if (!is.null(self$`match_expressions`)) lapply(self$`match_expressions`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1ScopeSelector$from_list <- function(x) {
  V1ScopeSelector$new(
    `match_expressions` = if (is.null(x[["matchExpressions"]])) NULL else lapply(x[["matchExpressions"]], V1ScopedResourceSelectorRequirement$from_list)
  )
}

