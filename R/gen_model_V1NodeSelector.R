# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1NodeSelector
#'
#' A node selector represents the union of the results of one or more label queries over a set of nodes; that is, it represents the OR of the selectors represented by the node selector terms.
#' @name V1NodeSelector
#' @noMd
#' @export
V1NodeSelector <- R6::R6Class(
  "V1NodeSelector",
  public = list(
    `node_selector_terms` = NULL
    ,
    #' @description Construct.
    initialize = function(`node_selector_terms` = NULL) {
      self$`node_selector_terms` <- `node_selector_terms`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["nodeSelectorTerms"]] <- if (!is.null(self$`node_selector_terms`)) lapply(self$`node_selector_terms`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1NodeSelector$from_list <- function(x) {
  V1NodeSelector$new(
    `node_selector_terms` = if (is.null(x[["nodeSelectorTerms"]])) NULL else lapply(x[["nodeSelectorTerms"]], V1NodeSelectorTerm$from_list)
  )
}

V1NodeSelector$required_fields <- c("nodeSelectorTerms")

