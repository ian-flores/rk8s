# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1ListMeta
#'
#' ListMeta describes metadata that synthetic resources must have, including lists and various status objects. A resource may have only one of {ObjectMeta, ListMeta}.
#' @name V1ListMeta
#' @noMd
#' @export
V1ListMeta <- R6::R6Class(
  "V1ListMeta",
  public = list(
    `continue` = NULL,
    `remaining_item_count` = NULL,
    `resource_version` = NULL,
    `self_link` = NULL
    ,
    #' @description Construct.
    initialize = function(`continue` = NULL, `remaining_item_count` = NULL, `resource_version` = NULL, `self_link` = NULL) {
      self$`continue` <- `continue`
      self$`remaining_item_count` <- `remaining_item_count`
      self$`resource_version` <- `resource_version`
      self$`self_link` <- `self_link`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["continue"]] <- self$`continue`
      out[["remainingItemCount"]] <- self$`remaining_item_count`
      out[["resourceVersion"]] <- self$`resource_version`
      out[["selfLink"]] <- self$`self_link`
      drop_nulls_deep(out)
    }
  )
)

V1ListMeta$from_list <- function(x) {
  V1ListMeta$new(
    `continue` = x[["continue"]],
    `remaining_item_count` = x[["remainingItemCount"]],
    `resource_version` = x[["resourceVersion"]],
    `self_link` = x[["selfLink"]]
  )
}

