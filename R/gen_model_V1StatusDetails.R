# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1StatusDetails
#'
#' StatusDetails is a set of additional properties that MAY be set by the server to provide additional information about a response. The Reason field of a Status object defines what attributes will be set. Clients must ignore fields that do not match the defined type of each attribute, and should assume that any attribute may be empty, invalid, or under defined.
#' @name V1StatusDetails
#' @noMd
#' @export
V1StatusDetails <- R6::R6Class(
  "V1StatusDetails",
  public = list(
    `causes` = NULL,
    `group` = NULL,
    `kind` = NULL,
    `name` = NULL,
    `retry_after_seconds` = NULL,
    `uid` = NULL
    ,
    #' @description Construct.
    initialize = function(`causes` = NULL, `group` = NULL, `kind` = NULL, `name` = NULL, `retry_after_seconds` = NULL, `uid` = NULL) {
      self$`causes` <- `causes`
      self$`group` <- `group`
      self$`kind` <- `kind`
      self$`name` <- `name`
      self$`retry_after_seconds` <- `retry_after_seconds`
      self$`uid` <- `uid`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["causes"]] <- if (!is.null(self$`causes`)) lapply(self$`causes`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["group"]] <- self$`group`
      out[["kind"]] <- self$`kind`
      out[["name"]] <- self$`name`
      out[["retryAfterSeconds"]] <- self$`retry_after_seconds`
      out[["uid"]] <- self$`uid`
      drop_nulls_deep(out)
    }
  )
)

V1StatusDetails$from_list <- function(x) {
  V1StatusDetails$new(
    `causes` = if (is.null(x[["causes"]])) NULL else lapply(x[["causes"]], V1StatusCause$from_list),
    `group` = x[["group"]],
    `kind` = x[["kind"]],
    `name` = x[["name"]],
    `retry_after_seconds` = x[["retryAfterSeconds"]],
    `uid` = x[["uid"]]
  )
}

