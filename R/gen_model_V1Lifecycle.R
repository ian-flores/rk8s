# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1Lifecycle
#'
#' Lifecycle describes actions that the management system should take in response to container lifecycle events. For the PostStart and PreStop lifecycle handlers, management of the container blocks until the action is complete, unless the container process fails, in which case the handler is aborted.
#' @name V1Lifecycle
#' @noMd
#' @export
V1Lifecycle <- R6::R6Class(
  "V1Lifecycle",
  public = list(
    `post_start` = NULL,
    `pre_stop` = NULL
    ,
    #' @description Construct.
    initialize = function(`post_start` = NULL, `pre_stop` = NULL) {
      self$`post_start` <- `post_start`
      self$`pre_stop` <- `pre_stop`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["postStart"]] <- if (!is.null(self$`post_start`)) self$`post_start`$to_list() else NULL
      out[["preStop"]] <- if (!is.null(self$`pre_stop`)) self$`pre_stop`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V1Lifecycle$from_list <- function(x) {
  V1Lifecycle$new(
    `post_start` = if (is.null(x[["postStart"]])) NULL else V1LifecycleHandler$from_list(x[["postStart"]]),
    `pre_stop` = if (is.null(x[["preStop"]])) NULL else V1LifecycleHandler$from_list(x[["preStop"]])
  )
}

