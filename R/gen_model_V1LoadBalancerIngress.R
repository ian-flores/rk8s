# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1LoadBalancerIngress
#'
#' LoadBalancerIngress represents the status of a load-balancer ingress point: traffic intended for the service should be sent to an ingress point.
#' @name V1LoadBalancerIngress
#' @noMd
#' @export
V1LoadBalancerIngress <- R6::R6Class(
  "V1LoadBalancerIngress",
  public = list(
    `hostname` = NULL,
    `ip` = NULL,
    `ip_mode` = NULL,
    `ports` = NULL
    ,
    #' @description Construct.
    initialize = function(`hostname` = NULL, `ip` = NULL, `ip_mode` = NULL, `ports` = NULL) {
      self$`hostname` <- `hostname`
      self$`ip` <- `ip`
      self$`ip_mode` <- `ip_mode`
      self$`ports` <- `ports`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["hostname"]] <- self$`hostname`
      out[["ip"]] <- self$`ip`
      out[["ipMode"]] <- self$`ip_mode`
      out[["ports"]] <- if (!is.null(self$`ports`)) lapply(self$`ports`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1LoadBalancerIngress$from_list <- function(x) {
  V1LoadBalancerIngress$new(
    `hostname` = x[["hostname"]],
    `ip` = x[["ip"]],
    `ip_mode` = x[["ipMode"]],
    `ports` = if (is.null(x[["ports"]])) NULL else lapply(x[["ports"]], V1PortStatus$from_list)
  )
}

