# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1ConfigMapVolumeSource
#'
#' Adapts a ConfigMap into a volume.
#' 
#' The contents of the target ConfigMap's Data field will be presented in a volume as files using the keys in the Data field as the file names, unless the items element is populated with specific mappings of keys to paths. ConfigMap volumes support ownership management and SELinux relabeling.
#' @name V1ConfigMapVolumeSource
#' @noMd
#' @export
V1ConfigMapVolumeSource <- R6::R6Class(
  "V1ConfigMapVolumeSource",
  public = list(
    `default_mode` = NULL,
    `items` = NULL,
    `name` = NULL,
    `optional` = NULL
    ,
    #' @description Construct.
    initialize = function(`default_mode` = NULL, `items` = NULL, `name` = NULL, `optional` = NULL) {
      self$`default_mode` <- `default_mode`
      self$`items` <- `items`
      self$`name` <- `name`
      self$`optional` <- `optional`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["defaultMode"]] <- self$`default_mode`
      out[["items"]] <- if (!is.null(self$`items`)) lapply(self$`items`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["name"]] <- self$`name`
      out[["optional"]] <- self$`optional`
      drop_nulls_deep(out)
    }
  )
)

V1ConfigMapVolumeSource$from_list <- function(x) {
  V1ConfigMapVolumeSource$new(
    `default_mode` = x[["defaultMode"]],
    `items` = if (is.null(x[["items"]])) NULL else lapply(x[["items"]], V1KeyToPath$from_list),
    `name` = x[["name"]],
    `optional` = x[["optional"]]
  )
}

