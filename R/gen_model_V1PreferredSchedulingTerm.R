# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1PreferredSchedulingTerm
#'
#' An empty preferred scheduling term matches all objects with implicit weight 0 (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
#' @name V1PreferredSchedulingTerm
#' @noMd
#' @export
V1PreferredSchedulingTerm <- R6::R6Class(
  "V1PreferredSchedulingTerm",
  public = list(
    `preference` = NULL,
    `weight` = NULL
    ,
    #' @description Construct.
    initialize = function(`preference` = NULL, `weight` = NULL) {
      self$`preference` <- `preference`
      self$`weight` <- `weight`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["preference"]] <- if (!is.null(self$`preference`)) self$`preference`$to_list() else NULL
      out[["weight"]] <- self$`weight`
      drop_nulls_deep(out)
    }
  )
)

V1PreferredSchedulingTerm$from_list <- function(x) {
  V1PreferredSchedulingTerm$new(
    `preference` = if (is.null(x[["preference"]])) NULL else V1NodeSelectorTerm$from_list(x[["preference"]]),
    `weight` = x[["weight"]]
  )
}

V1PreferredSchedulingTerm$required_fields <- c("weight", "preference")

