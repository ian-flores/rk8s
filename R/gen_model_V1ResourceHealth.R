# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1ResourceHealth
#'
#' ResourceHealth represents the health of a resource. It has the latest device health information. This is a part of KEP https://kep.k8s.io/4680 and historical health changes are planned to be added in future iterations of a KEP.
#' @name V1ResourceHealth
#' @noMd
#' @export
V1ResourceHealth <- R6::R6Class(
  "V1ResourceHealth",
  public = list(
    `health` = NULL,
    `resource_id` = NULL
    ,
    #' @description Construct.
    initialize = function(`health` = NULL, `resource_id` = NULL) {
      self$`health` <- `health`
      self$`resource_id` <- `resource_id`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["health"]] <- self$`health`
      out[["resourceID"]] <- self$`resource_id`
      drop_nulls_deep(out)
    }
  )
)

V1ResourceHealth$from_list <- function(x) {
  V1ResourceHealth$new(
    `health` = x[["health"]],
    `resource_id` = x[["resourceID"]]
  )
}

V1ResourceHealth$required_fields <- c("resourceID")

