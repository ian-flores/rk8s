# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1PodAffinityTerm
#'
#' Defines a set of pods (namely those matching the labelSelector relative to the given namespace(s)) that this pod should be co-located (affinity) or not co-located (anti-affinity) with, where co-located is defined as running on a node whose value of the label with key <topologyKey> matches that of any node on which a pod of the set of pods is running
#' @name V1PodAffinityTerm
#' @noMd
#' @export
V1PodAffinityTerm <- R6::R6Class(
  "V1PodAffinityTerm",
  public = list(
    `label_selector` = NULL,
    `match_label_keys` = NULL,
    `mismatch_label_keys` = NULL,
    `namespace_selector` = NULL,
    `namespaces` = NULL,
    `topology_key` = NULL
    ,
    #' @description Construct.
    initialize = function(`label_selector` = NULL, `match_label_keys` = NULL, `mismatch_label_keys` = NULL, `namespace_selector` = NULL, `namespaces` = NULL, `topology_key` = NULL) {
      self$`label_selector` <- `label_selector`
      self$`match_label_keys` <- `match_label_keys`
      self$`mismatch_label_keys` <- `mismatch_label_keys`
      self$`namespace_selector` <- `namespace_selector`
      self$`namespaces` <- `namespaces`
      self$`topology_key` <- `topology_key`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["labelSelector"]] <- if (!is.null(self$`label_selector`)) self$`label_selector`$to_list() else NULL
      out[["matchLabelKeys"]] <- self$`match_label_keys`
      out[["mismatchLabelKeys"]] <- self$`mismatch_label_keys`
      out[["namespaceSelector"]] <- if (!is.null(self$`namespace_selector`)) self$`namespace_selector`$to_list() else NULL
      out[["namespaces"]] <- self$`namespaces`
      out[["topologyKey"]] <- self$`topology_key`
      drop_nulls_deep(out)
    }
  )
)

V1PodAffinityTerm$from_list <- function(x) {
  V1PodAffinityTerm$new(
    `label_selector` = if (is.null(x[["labelSelector"]])) NULL else V1LabelSelector$from_list(x[["labelSelector"]]),
    `match_label_keys` = x[["matchLabelKeys"]],
    `mismatch_label_keys` = x[["mismatchLabelKeys"]],
    `namespace_selector` = if (is.null(x[["namespaceSelector"]])) NULL else V1LabelSelector$from_list(x[["namespaceSelector"]]),
    `namespaces` = x[["namespaces"]],
    `topology_key` = x[["topologyKey"]]
  )
}

V1PodAffinityTerm$required_fields <- c("topologyKey")

