# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1LabelSelector
#'
#' A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects.
#' @name V1LabelSelector
#' @noMd
#' @export
V1LabelSelector <- R6::R6Class(
  "V1LabelSelector",
  public = list(
    `match_expressions` = NULL,
    `match_labels` = NULL
    ,
    #' @description Construct.
    initialize = function(`match_expressions` = NULL, `match_labels` = NULL) {
      self$`match_expressions` <- `match_expressions`
      self$`match_labels` <- `match_labels`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["matchExpressions"]] <- if (!is.null(self$`match_expressions`)) lapply(self$`match_expressions`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["matchLabels"]] <- self$`match_labels`
      drop_nulls_deep(out)
    }
  )
)

V1LabelSelector$from_list <- function(x) {
  V1LabelSelector$new(
    `match_expressions` = if (is.null(x[["matchExpressions"]])) NULL else lapply(x[["matchExpressions"]], V1LabelSelectorRequirement$from_list),
    `match_labels` = x[["matchLabels"]]
  )
}

