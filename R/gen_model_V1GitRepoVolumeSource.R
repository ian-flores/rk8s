# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1GitRepoVolumeSource
#'
#' Represents a volume that is populated with the contents of a git repository. Git repo volumes do not support ownership management. Git repo volumes support SELinux relabeling.
#' 
#' DEPRECATED: GitRepo is deprecated. To provision a container with a git repo, mount an EmptyDir into an InitContainer that clones the repo using git, then mount the EmptyDir into the Pod's container.
#' @name V1GitRepoVolumeSource
#' @noMd
#' @export
V1GitRepoVolumeSource <- R6::R6Class(
  "V1GitRepoVolumeSource",
  public = list(
    `directory` = NULL,
    `repository` = NULL,
    `revision` = NULL
    ,
    #' @description Construct.
    initialize = function(`directory` = NULL, `repository` = NULL, `revision` = NULL) {
      self$`directory` <- `directory`
      self$`repository` <- `repository`
      self$`revision` <- `revision`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["directory"]] <- self$`directory`
      out[["repository"]] <- self$`repository`
      out[["revision"]] <- self$`revision`
      drop_nulls_deep(out)
    }
  )
)

V1GitRepoVolumeSource$from_list <- function(x) {
  V1GitRepoVolumeSource$new(
    `directory` = x[["directory"]],
    `repository` = x[["repository"]],
    `revision` = x[["revision"]]
  )
}

V1GitRepoVolumeSource$required_fields <- c("repository")

