# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1beta3ResourcePolicyRule
#'
#' ResourcePolicyRule is a predicate that matches some resource requests, testing the request's verb and the target resource. A ResourcePolicyRule matches a resource request if and only if: (a) at least one member of verbs matches the request, (b) at least one member of apiGroups matches the request, (c) at least one member of resources matches the request, and (d) either (d1) the request does not specify a namespace (i.e., `Namespace==""`) and clusterScope is true or (d2) the request specifies a namespace and least one member of namespaces matches the request's namespace.
#' @name V1beta3ResourcePolicyRule
#' @noMd
#' @export
V1beta3ResourcePolicyRule <- R6::R6Class(
  "V1beta3ResourcePolicyRule",
  public = list(
    `api_groups` = NULL,
    `cluster_scope` = NULL,
    `namespaces` = NULL,
    `resources` = NULL,
    `verbs` = NULL
    ,
    #' @description Construct.
    initialize = function(`api_groups` = NULL, `cluster_scope` = NULL, `namespaces` = NULL, `resources` = NULL, `verbs` = NULL) {
      self$`api_groups` <- `api_groups`
      self$`cluster_scope` <- `cluster_scope`
      self$`namespaces` <- `namespaces`
      self$`resources` <- `resources`
      self$`verbs` <- `verbs`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["apiGroups"]] <- self$`api_groups`
      out[["clusterScope"]] <- self$`cluster_scope`
      out[["namespaces"]] <- self$`namespaces`
      out[["resources"]] <- self$`resources`
      out[["verbs"]] <- self$`verbs`
      drop_nulls_deep(out)
    }
  )
)

V1beta3ResourcePolicyRule$from_list <- function(x) {
  V1beta3ResourcePolicyRule$new(
    `api_groups` = x[["apiGroups"]],
    `cluster_scope` = x[["clusterScope"]],
    `namespaces` = x[["namespaces"]],
    `resources` = x[["resources"]],
    `verbs` = x[["verbs"]]
  )
}

V1beta3ResourcePolicyRule$required_fields <- c("verbs", "apiGroups", "resources")

