# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1ControllerRevision
#'
#' ControllerRevision implements an immutable snapshot of state data. Clients are responsible for serializing and deserializing the objects that contain their internal state. Once a ControllerRevision has been successfully created, it can not be updated. The API Server will fail validation of all requests that attempt to mutate the Data field. ControllerRevisions may, however, be deleted. Note that, due to its use by both the DaemonSet and StatefulSet controllers for update and rollback, this object is beta. However, it may be subject to name and representation changes in future releases, and clients should not depend on its stability. It is primarily for internal use by controllers.
#' @name V1ControllerRevision
#' @noMd
#' @export
V1ControllerRevision <- R6::R6Class(
  "V1ControllerRevision",
  public = list(
    `api_version` = NULL,
    `data` = NULL,
    `kind` = NULL,
    `metadata` = NULL,
    `revision` = NULL
    ,
    #' @description Construct.
    initialize = function(`api_version` = NULL, `data` = NULL, `kind` = NULL, `metadata` = NULL, `revision` = NULL) {
      self$`api_version` <- `api_version`
      self$`data` <- `data`
      self$`kind` <- `kind`
      self$`metadata` <- `metadata`
      self$`revision` <- `revision`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["apiVersion"]] <- self$`api_version`
      out[["data"]] <- if (!is.null(self$`data`)) self$`data`$to_list() else NULL
      out[["kind"]] <- self$`kind`
      out[["metadata"]] <- if (!is.null(self$`metadata`)) self$`metadata`$to_list() else NULL
      out[["revision"]] <- self$`revision`
      drop_nulls_deep(out)
    }
  )
)

V1ControllerRevision$from_list <- function(x) {
  V1ControllerRevision$new(
    `api_version` = x[["apiVersion"]],
    `data` = if (is.null(x[["data"]])) NULL else RawExtension$from_list(x[["data"]]),
    `kind` = x[["kind"]],
    `metadata` = if (is.null(x[["metadata"]])) NULL else V1ObjectMeta$from_list(x[["metadata"]]),
    `revision` = x[["revision"]]
  )
}

V1ControllerRevision$required_fields <- c("revision")

