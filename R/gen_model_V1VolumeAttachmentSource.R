# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1VolumeAttachmentSource
#'
#' VolumeAttachmentSource represents a volume that should be attached. Right now only PersistenVolumes can be attached via external attacher, in future we may allow also inline volumes in pods. Exactly one member can be set.
#' @name V1VolumeAttachmentSource
#' @noMd
#' @export
V1VolumeAttachmentSource <- R6::R6Class(
  "V1VolumeAttachmentSource",
  public = list(
    `inline_volume_spec` = NULL,
    `persistent_volume_name` = NULL
    ,
    #' @description Construct.
    initialize = function(`inline_volume_spec` = NULL, `persistent_volume_name` = NULL) {
      self$`inline_volume_spec` <- `inline_volume_spec`
      self$`persistent_volume_name` <- `persistent_volume_name`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["inlineVolumeSpec"]] <- if (!is.null(self$`inline_volume_spec`)) self$`inline_volume_spec`$to_list() else NULL
      out[["persistentVolumeName"]] <- self$`persistent_volume_name`
      drop_nulls_deep(out)
    }
  )
)

V1VolumeAttachmentSource$from_list <- function(x) {
  V1VolumeAttachmentSource$new(
    `inline_volume_spec` = if (is.null(x[["inlineVolumeSpec"]])) NULL else V1PersistentVolumeSpec$from_list(x[["inlineVolumeSpec"]]),
    `persistent_volume_name` = x[["persistentVolumeName"]]
  )
}

