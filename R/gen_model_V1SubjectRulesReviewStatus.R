# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1SubjectRulesReviewStatus
#'
#' SubjectRulesReviewStatus contains the result of a rules check. This check can be incomplete depending on the set of authorizers the server is configured with and any errors experienced during evaluation. Because authorization rules are additive, if a rule appears in a list it's safe to assume the subject has that permission, even if that list is incomplete.
#' @name V1SubjectRulesReviewStatus
#' @noMd
#' @export
V1SubjectRulesReviewStatus <- R6::R6Class(
  "V1SubjectRulesReviewStatus",
  public = list(
    `evaluation_error` = NULL,
    `incomplete` = NULL,
    `non_resource_rules` = NULL,
    `resource_rules` = NULL
    ,
    #' @description Construct.
    initialize = function(`evaluation_error` = NULL, `incomplete` = NULL, `non_resource_rules` = NULL, `resource_rules` = NULL) {
      self$`evaluation_error` <- `evaluation_error`
      self$`incomplete` <- `incomplete`
      self$`non_resource_rules` <- `non_resource_rules`
      self$`resource_rules` <- `resource_rules`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["evaluationError"]] <- self$`evaluation_error`
      out[["incomplete"]] <- self$`incomplete`
      out[["nonResourceRules"]] <- if (!is.null(self$`non_resource_rules`)) lapply(self$`non_resource_rules`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["resourceRules"]] <- if (!is.null(self$`resource_rules`)) lapply(self$`resource_rules`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1SubjectRulesReviewStatus$from_list <- function(x) {
  V1SubjectRulesReviewStatus$new(
    `evaluation_error` = x[["evaluationError"]],
    `incomplete` = x[["incomplete"]],
    `non_resource_rules` = if (is.null(x[["nonResourceRules"]])) NULL else lapply(x[["nonResourceRules"]], V1NonResourceRule$from_list),
    `resource_rules` = if (is.null(x[["resourceRules"]])) NULL else lapply(x[["resourceRules"]], V1ResourceRule$from_list)
  )
}

V1SubjectRulesReviewStatus$required_fields <- c("resourceRules", "nonResourceRules", "incomplete")

