# Generated by tools/gen/generate.R -- do not edit by hand.

#' V2HPAScalingRules
#'
#' HPAScalingRules configures the scaling behavior for one direction. These Rules are applied after calculating DesiredReplicas from metrics for the HPA. They can limit the scaling velocity by specifying scaling policies. They can prevent flapping by specifying the stabilization window, so that the number of replicas is not set instantly, instead, the safest value from the stabilization window is chosen.
#' @name V2HPAScalingRules
#' @noMd
#' @export
V2HPAScalingRules <- R6::R6Class(
  "V2HPAScalingRules",
  public = list(
    `policies` = NULL,
    `select_policy` = NULL,
    `stabilization_window_seconds` = NULL
    ,
    #' @description Construct.
    initialize = function(`policies` = NULL, `select_policy` = NULL, `stabilization_window_seconds` = NULL) {
      self$`policies` <- `policies`
      self$`select_policy` <- `select_policy`
      self$`stabilization_window_seconds` <- `stabilization_window_seconds`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["policies"]] <- if (!is.null(self$`policies`)) lapply(self$`policies`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["selectPolicy"]] <- self$`select_policy`
      out[["stabilizationWindowSeconds"]] <- self$`stabilization_window_seconds`
      drop_nulls_deep(out)
    }
  )
)

V2HPAScalingRules$from_list <- function(x) {
  V2HPAScalingRules$new(
    `policies` = if (is.null(x[["policies"]])) NULL else lapply(x[["policies"]], V2HPAScalingPolicy$from_list),
    `select_policy` = x[["selectPolicy"]],
    `stabilization_window_seconds` = x[["stabilizationWindowSeconds"]]
  )
}

