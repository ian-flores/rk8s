# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1NodeSelectorTerm
#'
#' A null or empty node selector term matches no objects. The requirements of them are ANDed. The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
#' @name V1NodeSelectorTerm
#' @noMd
#' @export
V1NodeSelectorTerm <- R6::R6Class(
  "V1NodeSelectorTerm",
  public = list(
    `match_expressions` = NULL,
    `match_fields` = NULL
    ,
    #' @description Construct.
    initialize = function(`match_expressions` = NULL, `match_fields` = NULL) {
      self$`match_expressions` <- `match_expressions`
      self$`match_fields` <- `match_fields`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["matchExpressions"]] <- if (!is.null(self$`match_expressions`)) lapply(self$`match_expressions`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["matchFields"]] <- if (!is.null(self$`match_fields`)) lapply(self$`match_fields`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1NodeSelectorTerm$from_list <- function(x) {
  V1NodeSelectorTerm$new(
    `match_expressions` = if (is.null(x[["matchExpressions"]])) NULL else lapply(x[["matchExpressions"]], V1NodeSelectorRequirement$from_list),
    `match_fields` = if (is.null(x[["matchFields"]])) NULL else lapply(x[["matchFields"]], V1NodeSelectorRequirement$from_list)
  )
}

