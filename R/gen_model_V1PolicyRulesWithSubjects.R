# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1PolicyRulesWithSubjects
#'
#' PolicyRulesWithSubjects prescribes a test that applies to a request to an apiserver. The test considers the subject making the request, the verb being requested, and the resource to be acted upon. This PolicyRulesWithSubjects matches a request if and only if both (a) at least one member of subjects matches the request and (b) at least one member of resourceRules or nonResourceRules matches the request.
#' @name V1PolicyRulesWithSubjects
#' @noMd
#' @export
V1PolicyRulesWithSubjects <- R6::R6Class(
  "V1PolicyRulesWithSubjects",
  public = list(
    `non_resource_rules` = NULL,
    `resource_rules` = NULL,
    `subjects` = NULL
    ,
    #' @description Construct.
    initialize = function(`non_resource_rules` = NULL, `resource_rules` = NULL, `subjects` = NULL) {
      self$`non_resource_rules` <- `non_resource_rules`
      self$`resource_rules` <- `resource_rules`
      self$`subjects` <- `subjects`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["nonResourceRules"]] <- if (!is.null(self$`non_resource_rules`)) lapply(self$`non_resource_rules`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["resourceRules"]] <- if (!is.null(self$`resource_rules`)) lapply(self$`resource_rules`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["subjects"]] <- if (!is.null(self$`subjects`)) lapply(self$`subjects`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1PolicyRulesWithSubjects$from_list <- function(x) {
  V1PolicyRulesWithSubjects$new(
    `non_resource_rules` = if (is.null(x[["nonResourceRules"]])) NULL else lapply(x[["nonResourceRules"]], V1NonResourcePolicyRule$from_list),
    `resource_rules` = if (is.null(x[["resourceRules"]])) NULL else lapply(x[["resourceRules"]], V1ResourcePolicyRule$from_list),
    `subjects` = if (is.null(x[["subjects"]])) NULL else lapply(x[["subjects"]], FlowcontrolV1Subject$from_list)
  )
}

V1PolicyRulesWithSubjects$required_fields <- c("subjects")

