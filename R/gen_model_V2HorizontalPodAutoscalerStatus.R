# Generated by tools/gen/generate.R -- do not edit by hand.

#' V2HorizontalPodAutoscalerStatus
#'
#' HorizontalPodAutoscalerStatus describes the current status of a horizontal pod autoscaler.
#' @name V2HorizontalPodAutoscalerStatus
#' @noMd
#' @export
V2HorizontalPodAutoscalerStatus <- R6::R6Class(
  "V2HorizontalPodAutoscalerStatus",
  public = list(
    `conditions` = NULL,
    `current_metrics` = NULL,
    `current_replicas` = NULL,
    `desired_replicas` = NULL,
    `last_scale_time` = NULL,
    `observed_generation` = NULL
    ,
    #' @description Construct.
    initialize = function(`conditions` = NULL, `current_metrics` = NULL, `current_replicas` = NULL, `desired_replicas` = NULL, `last_scale_time` = NULL, `observed_generation` = NULL) {
      self$`conditions` <- `conditions`
      self$`current_metrics` <- `current_metrics`
      self$`current_replicas` <- `current_replicas`
      self$`desired_replicas` <- `desired_replicas`
      self$`last_scale_time` <- `last_scale_time`
      self$`observed_generation` <- `observed_generation`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["conditions"]] <- if (!is.null(self$`conditions`)) lapply(self$`conditions`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["currentMetrics"]] <- if (!is.null(self$`current_metrics`)) lapply(self$`current_metrics`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["currentReplicas"]] <- self$`current_replicas`
      out[["desiredReplicas"]] <- self$`desired_replicas`
      out[["lastScaleTime"]] <- if (!is.null(self$`last_scale_time`)) self$`last_scale_time`$to_list() else NULL
      out[["observedGeneration"]] <- self$`observed_generation`
      drop_nulls_deep(out)
    }
  )
)

V2HorizontalPodAutoscalerStatus$from_list <- function(x) {
  V2HorizontalPodAutoscalerStatus$new(
    `conditions` = if (is.null(x[["conditions"]])) NULL else lapply(x[["conditions"]], V2HorizontalPodAutoscalerCondition$from_list),
    `current_metrics` = if (is.null(x[["currentMetrics"]])) NULL else lapply(x[["currentMetrics"]], V2MetricStatus$from_list),
    `current_replicas` = x[["currentReplicas"]],
    `desired_replicas` = x[["desiredReplicas"]],
    `last_scale_time` = if (is.null(x[["lastScaleTime"]])) NULL else V1Time$from_list(x[["lastScaleTime"]]),
    `observed_generation` = x[["observedGeneration"]]
  )
}

V2HorizontalPodAutoscalerStatus$required_fields <- c("desiredReplicas")

