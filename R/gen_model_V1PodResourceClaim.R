# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1PodResourceClaim
#'
#' PodResourceClaim references exactly one ResourceClaim, either directly or by naming a ResourceClaimTemplate which is then turned into a ResourceClaim for the pod.
#' 
#' It adds a name to it that uniquely identifies the ResourceClaim inside the Pod. Containers that need access to the ResourceClaim reference it with this name.
#' @name V1PodResourceClaim
#' @noMd
#' @export
V1PodResourceClaim <- R6::R6Class(
  "V1PodResourceClaim",
  public = list(
    `name` = NULL,
    `resource_claim_name` = NULL,
    `resource_claim_template_name` = NULL
    ,
    #' @description Construct.
    initialize = function(`name` = NULL, `resource_claim_name` = NULL, `resource_claim_template_name` = NULL) {
      self$`name` <- `name`
      self$`resource_claim_name` <- `resource_claim_name`
      self$`resource_claim_template_name` <- `resource_claim_template_name`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["name"]] <- self$`name`
      out[["resourceClaimName"]] <- self$`resource_claim_name`
      out[["resourceClaimTemplateName"]] <- self$`resource_claim_template_name`
      drop_nulls_deep(out)
    }
  )
)

V1PodResourceClaim$from_list <- function(x) {
  V1PodResourceClaim$new(
    `name` = x[["name"]],
    `resource_claim_name` = x[["resourceClaimName"]],
    `resource_claim_template_name` = x[["resourceClaimTemplateName"]]
  )
}

V1PodResourceClaim$required_fields <- c("name")

