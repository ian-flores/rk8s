# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1EndpointSubset
#'
#' EndpointSubset is a group of addresses with a common set of ports. The expanded set of endpoints is the Cartesian product of Addresses x Ports. For example, given:
#' 
#' 	{
#' 	  Addresses: {"ip": "10.10.1.1"}, {"ip": "10.10.2.2"},
#' 	  Ports:     {"name": "a", "port": 8675}, {"name": "b", "port": 309}
#' 	}
#' 
#' The resulting set of endpoints can be viewed as:
#' 
#' 	a:  10.10.1.1:8675, 10.10.2.2:8675 ,
#' 	b:  10.10.1.1:309, 10.10.2.2:309 
#' @name V1EndpointSubset
#' @noMd
#' @export
V1EndpointSubset <- R6::R6Class(
  "V1EndpointSubset",
  public = list(
    `addresses` = NULL,
    `not_ready_addresses` = NULL,
    `ports` = NULL
    ,
    #' @description Construct.
    initialize = function(`addresses` = NULL, `not_ready_addresses` = NULL, `ports` = NULL) {
      self$`addresses` <- `addresses`
      self$`not_ready_addresses` <- `not_ready_addresses`
      self$`ports` <- `ports`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["addresses"]] <- if (!is.null(self$`addresses`)) lapply(self$`addresses`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["notReadyAddresses"]] <- if (!is.null(self$`not_ready_addresses`)) lapply(self$`not_ready_addresses`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["ports"]] <- if (!is.null(self$`ports`)) lapply(self$`ports`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1EndpointSubset$from_list <- function(x) {
  V1EndpointSubset$new(
    `addresses` = if (is.null(x[["addresses"]])) NULL else lapply(x[["addresses"]], V1EndpointAddress$from_list),
    `not_ready_addresses` = if (is.null(x[["notReadyAddresses"]])) NULL else lapply(x[["notReadyAddresses"]], V1EndpointAddress$from_list),
    `ports` = if (is.null(x[["ports"]])) NULL else lapply(x[["ports"]], CoreV1EndpointPort$from_list)
  )
}

