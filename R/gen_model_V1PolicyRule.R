# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1PolicyRule
#'
#' PolicyRule holds information that describes a policy rule, but does not contain information about who the rule applies to or which namespace the rule applies to.
#' @name V1PolicyRule
#' @noMd
#' @export
V1PolicyRule <- R6::R6Class(
  "V1PolicyRule",
  public = list(
    `api_groups` = NULL,
    `non_resource_ur_ls` = NULL,
    `resource_names` = NULL,
    `resources` = NULL,
    `verbs` = NULL
    ,
    #' @description Construct.
    initialize = function(`api_groups` = NULL, `non_resource_ur_ls` = NULL, `resource_names` = NULL, `resources` = NULL, `verbs` = NULL) {
      self$`api_groups` <- `api_groups`
      self$`non_resource_ur_ls` <- `non_resource_ur_ls`
      self$`resource_names` <- `resource_names`
      self$`resources` <- `resources`
      self$`verbs` <- `verbs`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["apiGroups"]] <- self$`api_groups`
      out[["nonResourceURLs"]] <- self$`non_resource_ur_ls`
      out[["resourceNames"]] <- self$`resource_names`
      out[["resources"]] <- self$`resources`
      out[["verbs"]] <- self$`verbs`
      drop_nulls_deep(out)
    }
  )
)

V1PolicyRule$from_list <- function(x) {
  V1PolicyRule$new(
    `api_groups` = x[["apiGroups"]],
    `non_resource_ur_ls` = x[["nonResourceURLs"]],
    `resource_names` = x[["resourceNames"]],
    `resources` = x[["resources"]],
    `verbs` = x[["verbs"]]
  )
}

V1PolicyRule$required_fields <- c("verbs")

