# Generated by tools/gen/generate.R -- do not edit by hand.

#' V2ResourceMetricStatus
#'
#' ResourceMetricStatus indicates the current value of a resource metric known to Kubernetes, as specified in requests and limits, describing each pod in the current scale target (e.g. CPU or memory).  Such metrics are built in to Kubernetes, and have special scaling options on top of those available to normal per-pod metrics using the "pods" source.
#' @name V2ResourceMetricStatus
#' @noMd
#' @export
V2ResourceMetricStatus <- R6::R6Class(
  "V2ResourceMetricStatus",
  public = list(
    `current` = NULL,
    `name` = NULL
    ,
    #' @description Construct.
    initialize = function(`current` = NULL, `name` = NULL) {
      self$`current` <- `current`
      self$`name` <- `name`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["current"]] <- if (!is.null(self$`current`)) self$`current`$to_list() else NULL
      out[["name"]] <- self$`name`
      drop_nulls_deep(out)
    }
  )
)

V2ResourceMetricStatus$from_list <- function(x) {
  V2ResourceMetricStatus$new(
    `current` = if (is.null(x[["current"]])) NULL else V2MetricValueStatus$from_list(x[["current"]]),
    `name` = x[["name"]]
  )
}

V2ResourceMetricStatus$required_fields <- c("name", "current")

