# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1alpha3DeviceClaim
#'
#' DeviceClaim defines how to request devices with a ResourceClaim.
#' @name V1alpha3DeviceClaim
#' @noMd
#' @export
V1alpha3DeviceClaim <- R6::R6Class(
  "V1alpha3DeviceClaim",
  public = list(
    `config` = NULL,
    `constraints` = NULL,
    `requests` = NULL
    ,
    #' @description Construct.
    initialize = function(`config` = NULL, `constraints` = NULL, `requests` = NULL) {
      self$`config` <- `config`
      self$`constraints` <- `constraints`
      self$`requests` <- `requests`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["config"]] <- if (!is.null(self$`config`)) lapply(self$`config`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["constraints"]] <- if (!is.null(self$`constraints`)) lapply(self$`constraints`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["requests"]] <- if (!is.null(self$`requests`)) lapply(self$`requests`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1alpha3DeviceClaim$from_list <- function(x) {
  V1alpha3DeviceClaim$new(
    `config` = if (is.null(x[["config"]])) NULL else lapply(x[["config"]], V1alpha3DeviceClaimConfiguration$from_list),
    `constraints` = if (is.null(x[["constraints"]])) NULL else lapply(x[["constraints"]], V1alpha3DeviceConstraint$from_list),
    `requests` = if (is.null(x[["requests"]])) NULL else lapply(x[["requests"]], V1alpha3DeviceRequest$from_list)
  )
}

