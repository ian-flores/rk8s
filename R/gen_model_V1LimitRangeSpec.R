# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1LimitRangeSpec
#'
#' LimitRangeSpec defines a min/max usage limit for resources that match on kind.
#' @name V1LimitRangeSpec
#' @noMd
#' @export
V1LimitRangeSpec <- R6::R6Class(
  "V1LimitRangeSpec",
  public = list(
    `limits` = NULL
    ,
    #' @description Construct.
    initialize = function(`limits` = NULL) {
      self$`limits` <- `limits`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["limits"]] <- if (!is.null(self$`limits`)) lapply(self$`limits`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      drop_nulls_deep(out)
    }
  )
)

V1LimitRangeSpec$from_list <- function(x) {
  V1LimitRangeSpec$new(
    `limits` = if (is.null(x[["limits"]])) NULL else lapply(x[["limits"]], V1LimitRangeItem$from_list)
  )
}

V1LimitRangeSpec$required_fields <- c("limits")

