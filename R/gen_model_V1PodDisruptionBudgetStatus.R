# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1PodDisruptionBudgetStatus
#'
#' PodDisruptionBudgetStatus represents information about the status of a PodDisruptionBudget. Status may trail the actual state of a system.
#' @name V1PodDisruptionBudgetStatus
#' @noMd
#' @export
V1PodDisruptionBudgetStatus <- R6::R6Class(
  "V1PodDisruptionBudgetStatus",
  public = list(
    `conditions` = NULL,
    `current_healthy` = NULL,
    `desired_healthy` = NULL,
    `disrupted_pods` = NULL,
    `disruptions_allowed` = NULL,
    `expected_pods` = NULL,
    `observed_generation` = NULL
    ,
    #' @description Construct.
    initialize = function(`conditions` = NULL, `current_healthy` = NULL, `desired_healthy` = NULL, `disrupted_pods` = NULL, `disruptions_allowed` = NULL, `expected_pods` = NULL, `observed_generation` = NULL) {
      self$`conditions` <- `conditions`
      self$`current_healthy` <- `current_healthy`
      self$`desired_healthy` <- `desired_healthy`
      self$`disrupted_pods` <- `disrupted_pods`
      self$`disruptions_allowed` <- `disruptions_allowed`
      self$`expected_pods` <- `expected_pods`
      self$`observed_generation` <- `observed_generation`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["conditions"]] <- if (!is.null(self$`conditions`)) lapply(self$`conditions`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["currentHealthy"]] <- self$`current_healthy`
      out[["desiredHealthy"]] <- self$`desired_healthy`
      out[["disruptedPods"]] <- if (!is.null(self$`disrupted_pods`)) lapply(self$`disrupted_pods`, function(.x) if (is.null(.x)) NULL else .x$to_list()) else NULL
      out[["disruptionsAllowed"]] <- self$`disruptions_allowed`
      out[["expectedPods"]] <- self$`expected_pods`
      out[["observedGeneration"]] <- self$`observed_generation`
      drop_nulls_deep(out)
    }
  )
)

V1PodDisruptionBudgetStatus$from_list <- function(x) {
  V1PodDisruptionBudgetStatus$new(
    `conditions` = if (is.null(x[["conditions"]])) NULL else lapply(x[["conditions"]], V1Condition$from_list),
    `current_healthy` = x[["currentHealthy"]],
    `desired_healthy` = x[["desiredHealthy"]],
    `disrupted_pods` = if (is.null(x[["disruptedPods"]])) NULL else lapply(x[["disruptedPods"]], V1Time$from_list),
    `disruptions_allowed` = x[["disruptionsAllowed"]],
    `expected_pods` = x[["expectedPods"]],
    `observed_generation` = x[["observedGeneration"]]
  )
}

V1PodDisruptionBudgetStatus$required_fields <- c("disruptionsAllowed", "currentHealthy", "desiredHealthy", "expectedPods")

