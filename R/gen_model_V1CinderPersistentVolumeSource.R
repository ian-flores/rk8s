# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1CinderPersistentVolumeSource
#'
#' Represents a cinder volume resource in Openstack. A Cinder volume must exist before mounting to a container. The volume must also be in the same region as the kubelet. Cinder volumes support ownership management and SELinux relabeling.
#' @name V1CinderPersistentVolumeSource
#' @noMd
#' @export
V1CinderPersistentVolumeSource <- R6::R6Class(
  "V1CinderPersistentVolumeSource",
  public = list(
    `fs_type` = NULL,
    `read_only` = NULL,
    `secret_ref` = NULL,
    `volume_id` = NULL
    ,
    #' @description Construct.
    initialize = function(`fs_type` = NULL, `read_only` = NULL, `secret_ref` = NULL, `volume_id` = NULL) {
      self$`fs_type` <- `fs_type`
      self$`read_only` <- `read_only`
      self$`secret_ref` <- `secret_ref`
      self$`volume_id` <- `volume_id`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["fsType"]] <- self$`fs_type`
      out[["readOnly"]] <- self$`read_only`
      out[["secretRef"]] <- if (!is.null(self$`secret_ref`)) self$`secret_ref`$to_list() else NULL
      out[["volumeID"]] <- self$`volume_id`
      drop_nulls_deep(out)
    }
  )
)

V1CinderPersistentVolumeSource$from_list <- function(x) {
  V1CinderPersistentVolumeSource$new(
    `fs_type` = x[["fsType"]],
    `read_only` = x[["readOnly"]],
    `secret_ref` = if (is.null(x[["secretRef"]])) NULL else V1SecretReference$from_list(x[["secretRef"]]),
    `volume_id` = x[["volumeID"]]
  )
}

V1CinderPersistentVolumeSource$required_fields <- c("volumeID")

