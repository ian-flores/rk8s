# Generated by tools/gen/generate.R -- do not edit by hand.

#' EventsV1EventSeries
#'
#' EventSeries contain information on series of events, i.e. thing that was/is happening continuously for some time. How often to update the EventSeries is up to the event reporters. The default event reporter in "k8s.io/client-go/tools/events/event_broadcaster.go" shows how this struct is updated on heartbeats and can guide customized reporter implementations.
#' @name EventsV1EventSeries
#' @noMd
#' @export
EventsV1EventSeries <- R6::R6Class(
  "EventsV1EventSeries",
  public = list(
    `count` = NULL,
    `last_observed_time` = NULL
    ,
    #' @description Construct.
    initialize = function(`count` = NULL, `last_observed_time` = NULL) {
      self$`count` <- `count`
      self$`last_observed_time` <- `last_observed_time`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["count"]] <- self$`count`
      out[["lastObservedTime"]] <- if (!is.null(self$`last_observed_time`)) self$`last_observed_time`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

EventsV1EventSeries$from_list <- function(x) {
  EventsV1EventSeries$new(
    `count` = x[["count"]],
    `last_observed_time` = if (is.null(x[["lastObservedTime"]])) NULL else V1MicroTime$from_list(x[["lastObservedTime"]])
  )
}

EventsV1EventSeries$required_fields <- c("count", "lastObservedTime")

