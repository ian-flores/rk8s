# Generated by tools/gen/generate.R -- do not edit by hand.

#' V1VolumeAttachmentSpec
#'
#' VolumeAttachmentSpec is the specification of a VolumeAttachment request.
#' @name V1VolumeAttachmentSpec
#' @noMd
#' @export
V1VolumeAttachmentSpec <- R6::R6Class(
  "V1VolumeAttachmentSpec",
  public = list(
    `attacher` = NULL,
    `node_name` = NULL,
    `source` = NULL
    ,
    #' @description Construct.
    initialize = function(`attacher` = NULL, `node_name` = NULL, `source` = NULL) {
      self$`attacher` <- `attacher`
      self$`node_name` <- `node_name`
      self$`source` <- `source`
    },

    #' @description Convert to a plain named list (JSON keys).
    to_list = function() {
      out <- list()
      out[["attacher"]] <- self$`attacher`
      out[["nodeName"]] <- self$`node_name`
      out[["source"]] <- if (!is.null(self$`source`)) self$`source`$to_list() else NULL
      drop_nulls_deep(out)
    }
  )
)

V1VolumeAttachmentSpec$from_list <- function(x) {
  V1VolumeAttachmentSpec$new(
    `attacher` = x[["attacher"]],
    `node_name` = x[["nodeName"]],
    `source` = if (is.null(x[["source"]])) NULL else V1VolumeAttachmentSource$from_list(x[["source"]])
  )
}

V1VolumeAttachmentSpec$required_fields <- c("attacher", "source", "nodeName")

