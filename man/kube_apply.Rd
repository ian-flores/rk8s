% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kube_verbs.R
\name{kube_apply}
\alias{kube_apply}
\title{Server-side apply a manifest}
\usage{
kube_apply(
  k,
  manifest,
  namespace = k$namespace,
  field_manager = k$field_manager,
  force = FALSE
)
}
\arguments{
\item{k}{A \link{Kube} session.}

\item{manifest}{Path, string, list, or model object.}

\item{namespace}{Default namespace for documents that don't specify one.}

\item{field_manager}{Field manager (defaults to \code{k$field_manager}).}

\item{force}{Resolve conflicts by overwriting other field managers.}
}
\value{
Invisibly, a list of server responses (one per document).
}
\description{
Accepts a YAML/JSON file path, a YAML/JSON string, an R \code{list}, or a
generated model (e.g. \code{V1Deployment}). Sends a PATCH with
\code{Content-Type: application/apply-patch+yaml}, which is k8s
"server-side apply". Multi-document YAML is supported.
}
\examples{
\dontrun{
k <- kube()
kube_apply(k, "deployment.yaml")
kube_apply(k, list(
  apiVersion = "v1", kind = "ConfigMap",
  metadata = list(name = "demo"),
  data = list(hello = "world")
))
}
}
