% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portforward.R
\name{pod_port_forward_open}
\alias{pod_port_forward_open}
\title{Open a port-forward session to a pod}
\usage{
pod_port_forward_open(client, namespace, name, ports)
}
\arguments{
\item{client}{An \link{ApiClient}.}

\item{namespace, name}{Pod coordinates.}

\item{ports}{Integer vector of remote ports to forward (e.g. \code{c(80, 443)}).}
}
\value{
A \link{PodPortForwardSession}.
}
\description{
Establishes a WebSocket to \verb{/api/v1/namespaces/\{ns\}/pods/\{name\}/portforward}
and returns a \link{PodPortForwardSession}. Unlike \verb{kubectl port-forward}, this
does \strong{not} stand up a local TCP listener; instead it exposes a programmatic
\code{write(port, bytes)} / \code{on_data(port, fn)} API. That's the direct mapping
of the websocket protocol and matches the Python client's \code{PortForward}.
}
\examples{
\dontrun{
k <- kube()
pf <- pod_port_forward_open(k$client, "default", "nginx-abc", ports = 80)
pf$on_data(80, function(b) cat(rawToChar(b)))
pf$write(80, charToRaw("GET / HTTP/1.1\r\nHost: x\r\nConnection: close\r\n\r\n"))
pf$wait(timeout = 5)
pf$close()
}
}
