% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portforward.R
\name{pod_port_forward}
\alias{pod_port_forward}
\title{Forward a pod port to a local TCP listener (kubectl-style)}
\usage{
pod_port_forward(client, namespace, name, remote_port, local_port = 0)
}
\arguments{
\item{client}{An \link{ApiClient}.}

\item{namespace, name}{Pod coordinates.}

\item{remote_port}{Pod-side port to connect to.}

\item{local_port}{Local port to bind. \code{0} (default) picks a random
ephemeral port; the chosen port is exposed as \verb{$port} on the result.}
}
\value{
A \code{PortForwardBridge} with fields \code{port} (the local port),
\code{process} (the \code{callr} background handle), and a \verb{$close()} method.
}
\description{
Stands up a local TCP server bound to \verb{127.0.0.1} and bridges every
accepted connection to a port on a pod via a \link{PodPortForwardSession}
running in a \code{callr} child process. Returns immediately; the bridge
lives until you call \verb{$close()}.
}
\details{
Equivalent to \verb{kubectl port-forward pod/<name> <local>:<remote>}.
}
\examples{
\dontrun{
k <- kube()
fwd <- pod_port_forward(k$client, "default", "nginx-abc", remote_port = 80)
Sys.sleep(0.5)
httr2::request(paste0("http://127.0.0.1:", fwd$port)) |> httr2::req_perform()
fwd$close()
}
}
