% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream.R
\name{pod_exec_open}
\alias{pod_exec_open}
\title{Open an interactive exec session on a pod}
\usage{
pod_exec_open(
  client,
  namespace,
  name,
  command,
  container = NULL,
  stdin = FALSE,
  stdout = TRUE,
  stderr = TRUE,
  tty = FALSE
)
}
\arguments{
\item{client}{An \link{ApiClient} (typically \code{kube()$client}).}

\item{namespace}{Namespace of the pod.}

\item{name}{Pod name.}

\item{command}{Character vector — the command and its arguments.}

\item{container}{Optional container name (required if the pod has more
than one container).}

\item{stdin, stdout, stderr}{Logical; which streams to enable (default
stdout+stderr only).}

\item{tty}{Logical; allocate a TTY (combines stdout+stderr; supports
terminal resize events).}
}
\value{
A \link{PodExecSession}.
}
\description{
Establishes a WebSocket to \verb{/api/v1/namespaces/\{ns\}/pods/\{name\}/exec} using
the \code{v4.channel.k8s.io} subprotocol and returns a \link{PodExecSession} you can
write to / read from.
}
\details{
Requires the \code{websocket} package (a Suggests dependency). If it isn't
installed, \code{pod_exec_open()} errors with the install hint. Authentication
is forwarded via a bearer-token \code{Authorization} header — exec/attach over
websocket with client-certificate auth is not currently supported.
}
\examples{
\dontrun{
k <- kube()
s <- pod_exec_open(k$client, "default", "nginx-abc", c("/bin/sh"),
                    stdin = TRUE)
s$on_stdout(function(b) cat(rawToChar(b)))
s$write_stdin("ls -la\n")
s$close()
}
}
