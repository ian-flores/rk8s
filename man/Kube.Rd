% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kube.R
\name{kube}
\alias{kube}
\alias{Kube}
\title{Open a Kubernetes session}
\usage{
kube(
  config_file = NULL,
  context = NULL,
  namespace = "default",
  client = NULL,
  field_manager = "rk8s"
)
}
\arguments{
\item{config_file}{Passed to \code{\link[=load_kube_config]{load_kube_config()}}.}

\item{context}{Passed to \code{\link[=load_kube_config]{load_kube_config()}}.}

\item{namespace}{Default namespace for verbs that take one.}

\item{client}{Optional pre-built \link{ApiClient}. When given, \code{config_file}
and \code{context} are ignored.}

\item{field_manager}{Field manager used by \code{\link[=kube_apply]{kube_apply()}} (defaults to
\code{"rk8s"}).}
}
\value{
A \code{Kube} object.
}
\description{
Returns a \code{Kube} object — a thin wrapper over \link{ApiClient} and
\link{DynamicClient} that lets you write idiomatic R against the cluster
without touching the typed \verb{V1*} model classes:
}
\details{
\preformatted{
k <- kube()                                   # ~/.kube/config
kube_get(k, "pods", namespace = "default")
kube_apply(k, "deployment.yaml")
kube_logs(k, "nginx-abc", namespace = "default")
kube_delete(k, "deployment/nginx", namespace = "default")
kube_scale(k, "deployment", "nginx", replicas = 5, namespace = "default")
kube_watch(k, "pods", on_event = function(type, obj)
             message(type, " ", obj$metadata$name))
}

This is the high-level surface most users want. The typed classes
(\code{V1Pod}, \code{CoreV1Api}, …) and the \link{DynamicClient} still work and are
there when you need them — \code{Kube} is built on top of both.

Resource references can be:
\itemize{
\item a kind alias: \code{"pod"}, \code{"pods"}, \code{"po"}, \code{"deployment"}, \code{"deploy"},
\code{"svc"}, …
\item \code{"kind/name"}: \code{"deployment/nginx"}
\item an explicit \code{apiVersion+kind}: \code{"apps/v1:Deployment"}
\item a \code{c(api_version, kind)} vector: \code{c("apps/v1", "Deployment")}
}

Constructed via \code{\link[=kube]{kube()}}. Carries an \link{ApiClient}, a \link{DynamicClient},
a default namespace, and a kind-to-resource alias table.

Usually you don't call \code{Kube} methods directly; use the \verb{kube_*} verbs
which dispatch into here.
}
\section{Public fields}{
\if{html}{\out{<div class="r6-fields">}}
\describe{
\item{\code{client}}{The backing \link{ApiClient}.}

\item{\code{dynamic}}{The backing \link{DynamicClient}.}

\item{\code{namespace}}{Default namespace.}

\item{\code{field_manager}}{Field manager used by \code{kube_apply()}.}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-Kube-new}{\code{Kube$new()}}
\item \href{#method-Kube-resource}{\code{Kube$resource()}}
\item \href{#method-Kube-resource_and_name}{\code{Kube$resource_and_name()}}
\item \href{#method-Kube-clone}{\code{Kube$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Kube-new"></a>}}
\if{latex}{\out{\hypertarget{method-Kube-new}{}}}
\subsection{Method \code{new()}}{
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Kube$new(client, namespace = "default", field_manager = "rk8s")}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{client}}{An \link{ApiClient}.}

\item{\code{namespace}}{Default namespace.}

\item{\code{field_manager}}{Field manager.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Kube-resource"></a>}}
\if{latex}{\out{\hypertarget{method-Kube-resource}{}}}
\subsection{Method \code{resource()}}{
Resolve a resource ref to a \code{DynamicResource}.
Accepts: \code{"pod"}, \code{"pods"}, \code{"po"}, \code{"deployment/nginx"} (name is
captured separately via \code{parse_ref}), \code{"apps/v1:Deployment"}, or
a \code{c(api_version, kind)} vector.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Kube$resource(ref)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{ref}}{Resource reference.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Kube-resource_and_name"></a>}}
\if{latex}{\out{\hypertarget{method-Kube-resource_and_name}{}}}
\subsection{Method \code{resource_and_name()}}{
Parse \code{ref} and return both the resource and a name (if
one was embedded as \code{"kind/name"}).
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Kube$resource_and_name(ref)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{ref}}{Resource reference.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Kube-clone"></a>}}
\if{latex}{\out{\hypertarget{method-Kube-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Kube$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
