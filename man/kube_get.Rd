% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kube_verbs.R
\name{kube_get}
\alias{kube_get}
\title{Get one or many Kubernetes resources}
\usage{
kube_get(
  k,
  ref,
  name = NULL,
  namespace = k$namespace,
  label_selector = NULL,
  field_selector = NULL,
  ...
)
}
\arguments{
\item{k}{A \link{Kube} session.}

\item{ref}{Resource ref — kind alias (\code{"pods"}), \code{"kind/name"}, or
explicit \code{"apiVersion:Kind"} / \code{c(apiVersion, Kind)}.}

\item{name}{Optional resource name (overrides any name in \code{ref}).}

\item{namespace}{Namespace (defaults to \code{k$namespace}).}

\item{label_selector, field_selector}{Optional selectors for list calls.}

\item{...}{Extra query parameters passed through to the API.}
}
\value{
A list (one object when fetching by name; a \verb{*List} envelope
otherwise) carrying the server response. Wrap with \code{\link[=as.data.frame]{as.data.frame()}}
or \code{tibble::as_tibble()} for tabular display.
}
\description{
If \code{name} is given (or embedded in \code{ref} as \code{"kind/name"}), returns a
single object. Otherwise lists the collection.
}
\examples{
\dontrun{
k <- kube()
kube_get(k, "pods", namespace = "default")
kube_get(k, "pod", "nginx-abc", namespace = "default")
kube_get(k, "deployment/nginx", namespace = "default")
kube_get(k, "apps/v1:Deployment", namespace = "default")
}
}
