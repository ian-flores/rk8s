% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream.R
\name{pod_exec}
\alias{pod_exec}
\title{Run a command in a pod, capture stdout/stderr/exit code}
\usage{
pod_exec(
  client,
  namespace,
  name,
  command,
  container = NULL,
  stdin_data = NULL,
  timeout = 60
)
}
\arguments{
\item{client}{An \link{ApiClient} (typically \code{kube()$client}).}

\item{namespace}{Namespace of the pod.}

\item{name}{Pod name.}

\item{command}{Character vector — the command and its arguments.}

\item{container}{Optional container name (required if the pod has more
than one container).}

\item{stdin_data}{Optional character or raw vector to send on stdin
before closing the input channel.}

\item{timeout}{Wall-clock cap in seconds (default 60). Sessions that
exceed it are forcibly closed; \code{timed_out = TRUE} is set on the result.}
}
\value{
A list with \code{stdout} (character), \code{stderr} (character),
\code{exit_code} (integer or \code{NA}), and \code{timed_out} (logical).
}
\description{
Higher-level one-shot wrapper over \code{\link[=pod_exec_open]{pod_exec_open()}} for the common case:
"run this and tell me what happened". Blocks until the remote command
exits and the connection closes.
}
\examples{
\dontrun{
k <- kube()
r <- pod_exec(k$client, "default", "nginx-abc", c("ls", "-la", "/"))
cat(r$stdout); message("exit ", r$exit_code)
}
}
