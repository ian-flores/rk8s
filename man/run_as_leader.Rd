% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leader_election.R
\name{run_as_leader}
\alias{run_as_leader}
\title{Run as the leader under a Kubernetes Lease}
\usage{
run_as_leader(
  client,
  lease_name,
  lease_namespace,
  identity,
  on_started_leading,
  on_stopped_leading = function() NULL,
  on_new_leader = NULL,
  lease_duration_seconds = 15,
  renew_deadline_seconds = 10,
  retry_period_seconds = 2
)
}
\arguments{
\item{client}{An \link{ApiClient}.}

\item{lease_name}{Name of the Lease object (created if missing).}

\item{lease_namespace}{Namespace of the Lease.}

\item{identity}{Unique identifier for this caller (hostname + PID is a
common choice). Two callers with the same identity are considered the
same holder and will never contest each other.}

\item{on_started_leading}{Function called once, with no arguments, when
this caller acquires the lease.}

\item{on_stopped_leading}{Function called once, with no arguments, when
this caller loses the lease (always paired with \code{on_started_leading}).}

\item{on_new_leader}{Optional \verb{function(identity)} called whenever the
observed leader changes (including this caller becoming leader).}

\item{lease_duration_seconds}{How long a successful acquire is valid
(default 15s). Other callers will consider the lease expired after
this interval of no renewal.}

\item{renew_deadline_seconds}{How long to keep trying to renew before
giving up and calling \code{on_stopped_leading} (default 10s).}

\item{retry_period_seconds}{Base backoff between acquire attempts
(default 2s).}
}
\value{
Invisibly, \code{TRUE} if this caller ever held leadership.
}
\description{
Acquire-and-renew leader election against a \code{coordination.k8s.io/v1} Lease
object. Exactly one caller across a cluster holds the lease at a time;
leadership is released automatically when the caller crashes or stops
renewing (within \code{lease_duration_seconds}).
}
\details{
Mirrors \code{kubernetes.leaderelection.LeaderElection} (Python) and
\code{client-go/tools/leaderelection}.

The typical caller pattern is a long-running controller: call
\code{run_as_leader()} on the main thread; the function blocks while this
caller is the leader and the \code{on_started_leading} callback is invoked
exactly once. When leadership is lost (lease is stolen, the API server
returns errors past the renew deadline, or \verb{$stop()} is called),
\code{on_stopped_leading} fires and the function returns.
}
\examples{
\dontrun{
client <- new_client_from_config()
run_as_leader(
  client,
  lease_name = "my-controller",
  lease_namespace = "kube-system",
  identity = paste0(Sys.info()[["nodename"]], "-", Sys.getpid()),
  on_started_leading = function() { message("leading"); controller_loop() },
  on_stopped_leading = function() message("stopped leading")
)
}
}
